# 营销方案新增「完全定制」分支 — 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 营销方案页新增「完全定制」Tab，用户自由输入富文本并植入活动要素占位符，C 端深度过滤后优先展示发布内容。

**Architecture:** 活动表新增 `customPromoHtml`(text) 字段，与现有 `promoModules` 并存。web 运营端 promo.vue 改双 Tab，完全定制用 RichEditor 编辑 + 占位符插入；C 端 shao detail.vue `customPromoHtml` 非空即优先渲染（占位符实时替换 + rich-text 过滤），为空回退模块组合。

**Tech Stack:** Strapi (zhao-point 插件)、uni-app H5 (web 运营端 + shao C 端)、Vue3 composition API。

**前置参照**：
- 设计文档：`docs/superpowers/specs/2026-08-28-营销方案完全定制-design.md`
- 占位符：`{{title}}` `{{startTime}}` `{{endTime}}` `{{venueName}}` `{{capacity}}` `{{cashPrice}}` `{{lecturer}}` `{{description}}`

---

## 文件结构

| 文件 | 职责 |
|---|---|
| `basic/plugins/zhao-point/server/src/content-types/activity/schema.json` | 新增 `customPromoHtml` 字段 |
| `web/src/pages/activity/promo.vue` | 双 Tab 改造 + 完全定制编辑区 + 占位符工具栏 |
| `shao/components/promo/promo-custom-page.vue`（新建） | C 端完全定制渲染组件（占位符替换 + rich-text） |
| `shao/pages/activity/detail.vue` | 宣传区块优先渲染 customPromoHtml，回退模块组合 |

---

### Task 1: 后端 schema 新增 customPromoHtml 字段

**Files:**
- Modify: `basic/plugins/zhao-point/server/src/content-types/activity/schema.json`

- [ ] **Step 1: 在 attributes 末尾新增字段**

在 `promoAssets` 之后追加：

```json
    "promoAssets": { "type": "json" },
    "customPromoHtml": { "type": "text" }
```

- [ ] **Step 2: 校验 schema 合法**

Run: `python -c "import json; json.load(open(r'e:\\code\\basic\\plugins\\zhao-point\\server\\src\\content-types\\activity\\schema.json')); print('OK')"`
Expected: `OK`

- [ ] **Step 3: Commit**

```bash
git add basic/plugins/zhao-point/server/src/content-types/activity/schema.json
git commit -m "feat(point): activity 新增 customPromoHtml 字段"
```

> ⚠️ 部署提醒：此 schema 变更需重新构建并部署 zhao-point 后端，否则数据库不创建新列（见部署指南第六、七步）。

---

### Task 2: web 运营端 promo.vue 改双 Tab

**Files:**
- Modify: `web/src/pages/activity/promo.vue`

- [ ] **Step 1: 引入 Tab 状态 + 占位符常量**

在 `<script setup>` 顶部（`form` 定义附近）新增：

```js
const activeScheme = ref('ai') // 'ai' | 'custom'

// 活动要素占位符工具栏（key 为占位符，label 为展示名）
const PLACEHOLDER_ITEMS = [
  { key: 'title', label: '标题' },
  { key: 'startTime', label: '开始时间' },
  { key: 'endTime', label: '结束时间' },
  { key: 'venueName', label: '场地' },
  { key: 'capacity', label: '名额' },
  { key: 'cashPrice', label: '报名费' },
  { key: 'lecturer', label: '讲师' },
  { key: 'description', label: '活动介绍' },
]

// 完全定制富文本内容（占位符原文存储）
const customPromoHtml = ref('')
```

同时把 `openPreview` 复用为完全定制预览（Tab2 也预览同一 promo-page）。

- [ ] **Step 2: 模板加双 Tab 切换**

在 `<template>` 的 `<template v-else-if="form.title">` 最前面、活动摘要之后插入：

```html
<view class="scheme-tabs">
  <view class="scheme-tab" :class="{ on: activeScheme === 'ai' }" @click="activeScheme = 'ai'">AI 智能生成</view>
  <view class="scheme-tab" :class="{ on: activeScheme === 'custom' }" @click="activeScheme = 'custom'">完全定制</view>
</view>

<!-- AI 智能生成：现有区块用 v-if 包裹 -->
<template v-if="activeScheme === 'ai'">
  ...现有 AI 生成区 / 活动介绍 / 配色 / 页面模块 / 联系方式...
</template>
```

> 操作说明：将现有「AI 生成宣传文案 / 活动介绍 / 配色方案 / 页面模块 / 联系方式」五个 `<view class="form-section">` 区块整体包裹进 `<template v-if="activeScheme === 'ai'">`。

- [ ] **Step 3: 新增完全定制区块（Tab2）**

在 Step 2 的 `<template v-if="activeScheme === 'ai'">` 结束后追加：

```html
<template v-else>
  <view class="form-section">
    <view class="section-title">完全定制文案</view>
    <view class="form-item">
      <text class="form-tip">自由排版编辑整页宣传内容。点击下方按钮在光标处插入活动要素，C 端会实时替换为最新活动信息。未填入的内容对应处显示为空。</text>

      <view class="placeholder-toolbar">
        <view
          v-for="p in PLACEHOLDER_ITEMS"
          :key="p.key"
          class="placeholder-chip"
          @click="insertPlaceholder(p.key)"
        >{{ p.label }}</view>
      </view>

      <RichEditor v-model="customPromoHtml" height="600rpx" placeholder="在此输入完全定制的宣传文案..." />
    </view>
  </view>
</template>
```

- [ ] **Step 4: 实现 insertPlaceholder + 加载/保存逻辑**

在 `genByAI` 附近添加：

```js
function insertPlaceholder(key) {
  customPromoHtml.value += '{{' + key + '}}'
  uni.showToast({ title: '已插入「' + key + '」占位符', icon: 'none' })
}
```

在 `save()` 中，Tab2 保存逻辑：

```js
async function save() {
  if (activeScheme.value === 'custom') {
    saving.value = true
    try {
      const data = { customPromoHtml: (customPromoHtml.value || '').trim() || null }
      await updateActivity(activityId.value, data)
      uni.showToast({ title: '已保存', icon: 'success' })
    } catch (e) {
      uni.showToast({ title: e.message || '保存失败', icon: 'none' })
    } finally {
      saving.value = false
    }
    return
  }
  // ...现有 Tab1 保存逻辑不变
}
```

在 `loadDetail()` 的 `Object.assign(form, {...})` 中补充回填：

```js
      promoAssets: Array.isArray(data.promoAssets) ? data.promoAssets : [],
      customPromoHtml: data.customPromoHtml || '',
```

并在 `data.promoContact` 之后为 `customPromoHtml` 赋值（reactive 中已定义）。

- [ ] **Step 5: 样式**

在 `<style scoped>` 末尾添加：

```css
.scheme-tabs { display: flex; gap: 16rpx; margin-bottom: 20rpx; }
.scheme-tab { flex: 1; text-align: center; padding: 20rpx 0; font-size: 28rpx; color: #666; background: #fff; border-radius: 12rpx; border: 1rpx solid #e3e6f0; }
.scheme-tab.on { color: #667eea; border-color: #667eea; background: rgba(102,126,234,.08); font-weight: bold; }
.placeholder-toolbar { display: flex; flex-wrap: wrap; gap: 12rpx; margin: 16rpx 0; }
.placeholder-chip { padding: 10rpx 22rpx; font-size: 24rpx; color: #667eea; background: #f0f4ff; border-radius: 30rpx; border: 1rpx solid #d6e0ff; }
```

- [ ] **Step 6: Commit**

```bash
git add web/src/pages/activity/promo.vue
git commit -m "feat(web): 宣传方案改双 Tab，新增完全定制编辑与占位符插入"
```

---

### Task 3: C 端完全定制渲染组件

**Files:**
- Create: `shao/components/promo/promo-custom-page.vue`

- [ ] **Step 1: 创建组件（占位符替换 + rich-text 过滤）**

```vue
<template>
  <view v-if="renderedHtml" class="promo-custom-page">
    <rich-text :nodes="renderedHtml" class="custom-rich" />
  </view>
</template>

<script setup>
import { computed } from 'vue'

const props = defineProps({
  activity: { type: Object, default: null },
})

// 活动要素占位符 → 实际值映射
const placeholderMap = computed(() => {
  const a = props.activity || {}
  const fmtTime = (v) => {
    if (!v) return ''
    const d = new Date(v)
    if (isNaN(d.getTime())) return String(v)
    const pad = (n) => String(n).padStart(2, '0')
    return `${d.getFullYear()}-${pad(d.getMonth() + 1)}-${pad(d.getDate())} ${pad(d.getHours())}:${pad(d.getMinutes())}`
  }
  const cost = Number(a.cashPrice ?? 0)
  return {
    title: a.title || '',
    startTime: fmtTime(a.startTime),
    endTime: fmtTime(a.endTime),
    venueName: a.venueName || (a.venue && a.venue.name) || '',
    capacity: a.capacity == null ? '' : String(a.capacity),
    cashPrice: cost > 0 ? String(cost) : '',
    lecturer: (a.lecturer && a.lecturer.name) || '',
    description: a.description || '',
  }
})

const renderedHtml = computed(() => {
  const raw = (props.activity && props.activity.customPromoHtml) || ''
  if (!raw) return ''
  // 实时替换占位符；未提供的替换为空串
  return raw.replace(/\{\{(\w+)\}\}/g, (m, key) => {
    return placeholderMap.value[key] !== undefined ? placeholderMap.value[key] : ''
  })
})
</script>

<style lang="scss" scoped>
.promo-custom-page { padding: 24rpx; }
.custom-rich { font-size: 28rpx; color: var(--c-text); line-height: 1.7; }
</style>
```

- [ ] **Step 2: Commit**

```bash
git add shao/components/promo/promo-custom-page.vue
git commit -m "feat(c端的): 完全定制宣传页渲染组件（占位符替换 + rich-text 过滤）"
```

---

### Task 4: C 端 detail.vue 优先渲染完全定制

**Files:**
- Modify: `shao/pages/activity/detail.vue`

- [ ] **Step 1: 引入组件**

在 import 区（`PromoCustom` 附近，约 436 行）新增：

```js
import PromoCustomPage from '../../components/promo/promo-custom-page.vue'
```

- [ ] **Step 2: 宣传区块优先渲染完全定制**

将宣传区块（目前是 `<view v-if="modules.length" class="promo-page ...">`）改为：

```html
<!-- 宣传文案：完全定制优先，否则回退模块组合（含主题配色） -->
<view
  v-if="customPromoHtml"
  class="promo-page promo-section"
  :class="promoClass"
  :style="colorVars"
>
  <PromoCustomPage :activity="activity" />
</view>
<view v-else-if="modules.length" class="promo-page promo-section" :class="promoClass" :style="colorVars">
  <block v-for="m in modules" :key="m.sort">
    ...现有模块渲染...
  </block>
</view>
```

- [ ] **Step 3: 新增计算属性**

在 `modules` computed 附近（约 800 行）新增：

```ts
const customPromoHtml = computed(() => {
  const v = activity.value?.customPromoHtml
  return v && String(v).trim() ? String(v) : ''
})
```

- [ ] **Step 4: Commit**

```bash
git add shao/pages/activity/detail.vue
git commit -m "feat(sha): 宣传页优先展示完全定制，为空回退模块组合"
```

---

## 自审结论

- **覆盖映射**：Tab 命名/双 Tab(Task2)、customPromoHtml 字段(Task1)、C 端优先判定+回退(Task4)、占位符实时替换(Task3)、rich-text 深度过滤(Task3)。全部 spec 需求覆盖。
- **占位符一致性**：`{{title}}/{{startTime}}/{{endTime}}/{{venueName}}/{{capacity}}/{{cashPrice}}/{{lecturer}}/{{description}}` 在 Task2 常量与 Task3 map 中 key 一致。
- **无占位符**：每步含具体代码与命令。
- **类型一致性**：`customPromoHtml` 命名在 schema/web/shao 三端统一。