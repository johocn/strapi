# 活动并发 / 幂等硬化 实施计划

设计：[2026-09-26-活动并发幂等硬化-design.md](file:///e:/code/basic/docs/superpowers/specs/2026-09-26-活动并发幂等硬化-design.md)
仓库：`basic`（zhao-point 插件）。零 DDL、零新依赖。

## T1 新增纯逻辑模块

文件：`plugins/zhao-point/server/src/services/activity-concurrency.ts`

```ts
/**
 * 活动并发/幂等硬化：把并发下的取舍抽成纯函数，便于单测锁死语义。
 * 所有函数不做 IO。
 */

/** Strapi updateMany/deleteMany 返回 {count:N}；归一为 number，异常形状回落 0（切勿对对象直接 Number） */
export function affectedCount(result: unknown): number {
  if (typeof result === "number") return Number.isFinite(result) ? result : 0;
  if (result && typeof result === "object" && "count" in (result as any)) {
    const n = Number((result as any).count);
    return Number.isFinite(n) ? n : 0;
  }
  return 0;
}

export interface CancelOutcome {
  proceed: boolean;         // 是否本次取消的赢家
  notify: boolean;          // 是否发取消通知
  refund: boolean;          // 是否退报名费
  releaseSeat: boolean;     // 是否释放名额
  promoteWaitlist: boolean; // 是否递补候补
}

/** 取消报名的并发认领结果 → 副作用取舍。后到者计数全 0，不得重复释放名额/退款/递补 */
export function cancelOutcome(counts: { active?: number; waiting?: number }): CancelOutcome {
  const active = Number(counts?.active) || 0;
  const waiting = Number(counts?.waiting) || 0;
  if (active === 0 && waiting === 0) {
    return { proceed: false, notify: false, refund: false, releaseSeat: false, promoteWaitlist: false };
  }
  const won = active > 0;
  return { proceed: true, notify: true, refund: won, releaseSeat: won, promoteWaitlist: won };
}
```

## T2 单测

文件：`plugins/zhao-point/tests/activity-concurrency.test.ts`
覆盖 `{count:N}` 对象形态 / 裸数字 / `null` / `undefined` / `{}` / `NaN`，以及 `cancelOutcome` 三态。

## T3 C1 签到一次性认领

`activity.ts` `checkin()`：geo 校验与 `resolveFee` 保留在事务外 → 事务内 `trx("activity_signups").forUpdate()` + 复读 attendance + 扣费 + create + `attendedAt` → 事务外 `grantPoints` / `pointsGranted` / 学习包授权。

## T4 C2 候补转正锚点锁

`activity.ts` 新增模块级 `promoteOneClaim(strapi, activityId, p, act)`；`promoteWaiting` 循环改为调用它，按 `promoted` / `stale` / `no_seat` 分流，积分与通知移出事务。

## T5 C3 取条件更新认领

`activity.ts` `cancel()`：`updateMany(active)` → 行数 0 再 `updateMany(waiting)` → `cancelOutcome` 决定副作用。

## T6 C4 报名原子认领

`activity.ts` `signup()`：765-825 段折进 `strapi.db.transaction`（锚点锁 activities）→ 返回 `{kind:"dup"|"waiting"|"active"}`，扣费失败抛错回滚后外层映射 `insufficient_points`；826-946 尾链不变。

## T7 C5 台账号段加锁

`activity-ledger.ts`：`generate` 尾段与 `generateAutoIfAbsent` 的 auto 存在性检查进 `activities` 锚点锁事务。

## T8 构建与自检

`cd plugins/zhao-point && npm run build` → Grep `dist/server` 命中 `affectedCount` 与 `cancelOutcome` → `npx jest tests/`。

## T9 部署与验证

只 add 本 Task 文件 → commit → push → `ssh joho` ff-only + `pm2 restart strapi` → curl 验收 → 自建临时数据做数据级并发验证 → 清理并复核残留 0 → 回填设计文档 §8 与项目记忆。