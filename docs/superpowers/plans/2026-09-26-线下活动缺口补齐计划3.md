# 线下活动缺口补齐计划 3 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 闭环演练暴露的 4 个新问题——P0 主键序列失同步（防复发）、P1 转正通知因模板缺失整条丢失、P2 站内信静默降级无日志、P3 造数语义记录。

**Architecture:** 主修代码韧性（通知按通道隔离 try/catch，微信失败不连坐站内信）+ 启动期幂等序列自愈兜底（沿用项目 bootstrap 兜底惯例）；模板补配作为运营数据一次性执行，不在代码内跨插件种子。

**Tech Stack:** Strapi 5 插件 zhao-point、jest、knex raw、PostgreSQL（容器 `1Panel-postgresql-pIe0`）

## 已确证事实（2026-09-26 侦察，可直接引用）

1. **P1 根因（代码）**：[activity.ts#L1756-L1778](file:///e:/code/basic/plugins/zhao-point/server/src/services/activity.ts#L1756-L1778) `notifyPromoted` 在**同一个 try 内**先 `msg.sendNow(...)`（微信，`templateCode: "act_promoted"`）再 `this.notifyInApp(...)`。sendNow 抛错 → 直接进 catch → **站内信从未发送**。
2. **P1 根因（数据）**：生产 `sso_msg_templates` 现有 6 行：`act_confirm`/`act_before`/`act_receipt`/`act_repurchase`/`act_noshow_revisit`/`admin_notify`，**无 `act_promoted`**。表字段：`code, name, provider, wx_template_id, wx_template_fields(jsonb), content, is_enabled, description, daily_cap, cooldown_minutes, locale`。其中仅 `act_confirm` 有 `wx_template_id`，其余为空——**sendNow 对空 template_id 的行为需 Task 1 先读代码确认**。
3. **P0 现状**：全库序列巡检（pg_get_serial_sequence 逐表比对 max(id)）**当前无任何 LAG**；`up_users` max=33=seq；`activity_signups` max=18=seq。演练期已 setval 修复，**根因未定、无防复发机制**。
4. **P2 现状**：`notifyPromoted` 对无 sso 映射已 warn（:1764），但演练报告另有静默降级点，Task 1 需定位（疑在 `broadcastAdminChange` 的 `if (sso)` 分支或无匹配时的路径）。
5. **P3**：签到「过去时段」造数需 start 过去 + end 未来（ongoing），否则被签到兜底拦截——纯文档记录，不写代码。
6. `point_records` 表不存在（演练子代理查询用错名），积分表名以 `zhao_point_*` 前缀为准，本计划不涉及积分表改动。

## 执行总序

Task 1（通知韧性代码）→ Task 2（序列自愈兜底）→ Task 3（生产模板补配 + P2 日志）→ Task 4（部署验证 + 记忆回填）。Task 1/2 均改 `server/src` + 需 `npm run build`，**必须串行**（dist 产物会互相覆盖）。

---

### Task 1: 通知通道隔离（P1-a 代码主修 + P2 日志）

**Files:**
- Modify: `plugins/zhao-point/server/src/services/activity.ts`（`notifyPromoted` L1756-1778，必要时 `broadcastAdminChange` L1782-1812）
- Test: `plugins/zhao-point/tests/activity-notice.test.ts`（追加）

- [ ] **Step 1.1: 先读 `msg.sendNow` 实现**（`plugins/zhao-sso/server/src/services/sso-msg.ts`），确认：模板 code 不存在时抛什么错、`wx_template_id` 为空时是否抛、`dedupeKey` 命中时的行为。把结论写进 commit message 或代码注释一句话。
- [ ] **Step 1.2: 追加失败单测**（mock sso-msg.sendNow 抛错 → 断言 `notifyInApp` 仍被调用一次；mock 无 sso 映射 → 断言有 warn 日志）。参考 `tests/activity-notice.test.ts` 既有 mock 风格（若既有测试直接测纯函数，则为本 Task 新建一个针对 service 方法的受控单测，mock `strapi.plugin("zhao-sso")` 返回抛出型 `service("sso-msg")`）。
- [ ] **Step 1.3: 改写 `notifyPromoted` 为通道隔离**（站内信优先且独立于微信）：

```ts
  /** 递补转正即时通知：站内信与微信按通道隔离，任一通道失败不得连坐另一通道。 */
  async notifyPromoted(upUserId: number, activityId: number) {
    const act = await strapi.db.query("plugin::zhao-point.activity").findOne({ where: { id: activityId } });
    if (!act) return;
    const params = { name: act.title, startTime: act.startTime };
    try {
      await this.notifyInApp(upUserId, activityId, "activity.promoted", params, `activity:promoted:${upUserId}:${activityId}`);
    } catch (e: any) {
      strapi.log.warn(`[zhao-point:activity] promote inapp notify failed (user=${upUserId}): ${e.message}`);
    }
    try {
      const sop = strapi.plugin("zhao-sso")?.service("sso-sop");
      const msg = strapi.plugin("zhao-sso")?.service("sso-msg");
      if (!sop || !msg) return;
      const sso = await sop.resolveSsoUserForUpUser(upUserId);
      if (!sso) {
        strapi.log.warn(`[zhao-point:activity] promote wx notify skip: no sso mapping for upUser=${upUserId}`);
        return;
      }
      await msg.sendNow({
        user: sso.id,
        scene: "activity.promoted",
        templateCode: "act_promoted",
        params: { name: act.title, time: act.startTime },
        dedupeKey: `activity:promote:${upUserId}:${activityId}`,
      });
    } catch (e: any) {
      strapi.log.warn(`[zhao-point:activity] promote wx notify failed (user=${upUserId}): ${e.message}`);
    }
  },
```

- [ ] **Step 1.4: 巡检 `broadcastAdminChange`**：同样把「站内信」与「微信」拆成两个独立 try（当前站内信在微信之前，站内信成功但微信失败仅 warn——已可用；但站内信失败会跳过微信，改为隔离更稳）。
- [ ] **Step 1.5: 定位并补 P2 静默点**：搜索 `resolveSsoUserForUpUser` 的全部调用点，凡返回 null 而**无 warn** 的补 `strapi.log.warn`（不改业务行为，仅可观测）。
- [ ] **Step 1.6: `npm test`**（新用例全绿；permissions.test.ts 21 例既有失败忽略）→ `npm run build` → `Select-String dist/server/index.js -Pattern "promote wx notify skip"` 命中 → commit（只 add activity.ts、测试文件、dist 产物）→ push origin main。**本 Task 不部署**（与 Task 2 合并部署）。

### Task 2: 启动期 ID 序列自愈兜底（P0）

**Files:**
- Modify: `plugins/zhao-point/server/src/bootstrap.ts`（新增 `healIdSequences`，放在既有幂等 DDL 兜底之后）

- [ ] **Step 2.1: 加函数**（幂等：仅 `last_value < max(id)` 时 setval；空表/正常表不动；返回明细供日志）：

```ts
/**
 * 启动期 ID 序列自愈：历史上存在显式 id 插入（导入/同步）导致 serial 序列落后于 max(id)，
 * 后续任何 insert 都会撞主键冲突（生产已实际发生：新用户注册 100% 失败）。
 * 幂等：仅当 last_value < max(id) 时 setval；每次启动巡检全部 public 表，修复明细打 warn。
 */
const healIdSequences = async (strapi: Core.Strapi) => {
  const knex = strapi.db.connection;
  const res: any = await knex.raw(`
    SELECT c.relname AS tbl,
           pg_get_serial_sequence('public.' || quote_ident(c.relname), 'id') AS seq
    FROM pg_class c
    JOIN pg_namespace n ON n.oid = c.relnamespace
    WHERE n.nspname = 'public' AND c.relkind = 'r'
  `);
  const list: Array<{ tbl: string; seq: string | null }> = (res?.rows ?? res) || [];
  const healed: string[] = [];
  for (const { tbl, seq } of list) {
    if (!seq) continue;
    const maxRes: any = await knex.raw("SELECT COALESCE(max(id), 0) AS mx FROM ??", [tbl]);
    const mx = Number(((maxRes?.rows ?? maxRes) || [])[0]?.mx ?? 0);
    const seqRes: any = await knex.raw(`SELECT last_value FROM ${seq}`);
    const lv = Number(((seqRes?.rows ?? seqRes) || [])[0]?.last_value ?? 0);
    if (lv < mx) {
      await knex.raw("SELECT setval(?, ?)", [seq, mx]);
      healed.push(`${tbl}:${lv}->${mx}`);
    }
  }
  if (healed.length > 0) {
    strapi.log.warn(`[zhao-point] 修复落后 ID 序列 ${healed.length} 个: ${healed.join(", ")}`);
  }
};
```

- [ ] **Step 2.2: 接线**（在既有报名唯一性兜底 / token 收紧之后）：

```ts
  try {
    await healIdSequences(strapi);
  } catch (err: any) {
    strapi.log.warn(`[zhao-point] ID 序列自愈巡检失败: ${err.message}`);
  }
```

- [ ] **Step 2.3: 根因排查（只读，写进 commit message）**：Grep `plugins/zhao-sso`、`plugins/zhao-auth`、`src/` 下所有创建用户的代码路径（`plugin::users-permissions.user` / `up_users`），判断是否存在带**显式 `id`** 的 create（如 SSO 同步、迁移脚本、种子）；有则一并修掉（去掉显式 id 或改用序列），无则记录「疑为历史一次性导入，已由自愈兜底覆盖」。
- [ ] **Step 2.4: `npm test` → `npm run build` → 验证 dist 含 `healIdSequences`** → commit（bootstrap.ts + dist）→ push origin main。
- [ ] **Step 2.5: 部署 + 验证**（Task 1+2 一起）：

```powershell
ssh joho "cd /www/apps/strapi && git diff --name-only HEAD origin/main | head -20 && git fetch && git merge --ff-only origin/main"
ssh joho "export PATH=/home/admin/.nvm/versions/node/v22.23.1/bin:/usr/bin:/bin:$PATH; export PM2_HOME=/home/admin/.pm2; pm2 restart strapi"
```
Expected：ff-only 成功（重叠则停止报告）；约 60s 后 `pm2 logs` 无「修复落后 ID 序列」（当前无 LAG，预期静默通过）、无「自愈巡检失败」；注册一个新用户成功（P0 回归验证）。

### Task 3: 生产补 `act_promoted` 模板 + P2 确认

**Files:** 无代码改动（生产数据一次性写入 + 结论记录到本计划「执行记录」）

- [ ] **Step 3.1: 依 Task 1.1 结论补模板**：`INSERT INTO sso_msg_templates (code, name, provider, wx_template_fields, content, is_enabled, description, created_at, updated_at, published_at, document_id, locale)` 插入 `act_promoted`，字段与同类（如 `act_confirm`）对齐；`wx_template_id` 留空（与其余 5 条一致，若 Step 1.1 证明空 id 会抛错则改为不补模板、仅依赖 Step 1.3 代码韧性，并在报告中说明）。写入前先 `SELECT count(*) WHERE code='act_promoted'` 确认不存在（幂等）。
- [ ] **Step 3.2: 验证**：重跑一次候补转正（或直接调 service 验证路径），确认站内信到达 + 微信按模板状态发送；若模板缺 id 仍抛，则证明代码韧性生效（站内信仍到达）——**两种情况都算通过，因为主修是代码隔离**。

### Task 4: 记忆回填与收尾

- [ ] **Step 4.1**: 本文件追加「执行记录」小节（Task1-3 结论、commit hash、验证证据）。
- [ ] **Step 4.2**: 更新 `project_memory.md`：P0 已加自愈兜底 + 根因结论；P1 代码已隔离（模板缺失不再连坐站内信）+ 模板补配状态；P2 已补 warn；P3 造数语义。
- [ ] **Step 4.3**: commit（本计划文件 + 记忆为仓库外文件不提交）→ push origin main。

**验收标准：** 微信通道抛错时站内信仍送达（单测覆盖）；启动日志无序列 LAG 且新用户注册成功；`act_promoted` 模板存在或明确记录不补理由；dist 已重建并部署验证；commit 只含本计划涉及文件。

**明确不做：** 不重构整个通知体系；不改 `sso_msg_templates` 表结构；不引入新依赖；不动演练遗留数据与测试用户。