# 活动标签对齐 + 聚合接口 实施计划（Plan A）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 activity.tags 从 JSON 对齐为 zhao-tag 统一标签 relation，新增 slug/relatedOverride/showRelatedSection 字段，提供 zhao-tag 聚合接口，管理端活动编辑页接入标签选择与相关内容配置。

**Architecture:** 后端 Strapi 插件 schema 变更（zhao-point activity + zhao-tag tag 逆侧）→ SQL 迁移脚本（JSON tags→tag 记录+relation）→ zhao-tag 新增公开聚合服务（按标签集对每类型各取 top N）→ 管理端表单改造。独立可 curl 验证。

**Tech Stack:** Strapi v5（strapi.documents API）、PostgreSQL、Vue3/HBuilder X（管理端）、Shell+psql（迁移）

依赖设计：`docs/superpowers/specs/2026-09-08-活动页标签联动-design.md`

---

### Task 1: activity/tag schema 变更

**Files:**
- Modify: `basic/plugins/zhao-point/server/src/content-types/activity/schema.json`
- Modify: `basic/plugins/zhao-tag/server/src/content-types/tag/schema.json`

- [ ] **Step 1: activity.tags 改为 relation，新增 3 字段**

修改 `activity/schema.json`：

```json
"tags": { "type": "json" },
```
改为：
```json
"tags": { "type": "relation", "relation": "manyToMany", "target": "plugin::zhao-tag.tag", "inversedBy": "pointActivities" },
```
在 `"promoTemplate"` 之前追加：
```json
"slug": { "type": "uid", "targetField": "title", "required": false },
"relatedOverride": { "type": "json" },
"showRelatedSection": { "type": "boolean", "default": true },
```

- [ ] **Step 2: zhao-tag.tag 补 activity 逆侧**

修改 `tag/schema.json`，在 `"website_products"` 后追加：

```json
"pointActivities": {
  "type": "relation",
  "relation": "manyToMany",
  "target": "plugin::zhao-point.activity",
  "mappedBy": "tags"
},
```

- [ ] **Step 3: 本地构建验证（2G 服务器禁止构建，必须本地）**

在 `basic/plugins/zhao-point/` 和 `basic/plugins/zhao-tag/` 下各执行：

```bash
npm run build
```
Expected: 无 schema/类型错误。确认 `dist/server/src/content-types/activity/schema.json` 包含 `"pointActivities"` 逆侧引用（grep 关键字）。

- [ ] **Step 4: Commit**

```bash
git add basic/plugins/zhao-point/server/src/content-types/activity/schema.json basic/plugins/zhao-tag/server/src/content-types/tag/schema.json
git add basic/plugins/zhao-point/dist basic/plugins/zhao-tag/dist
git commit -m "feat(zhao-point/zhao-tag): activity tags 对齐统一标签 relation，新增 slug/relatedOverride/showRelatedSection"
```

---

### Task 2: JSON tags → tag relation 迁移脚本

**Files:**
- Create: `basic/scripts/migrate_activity_tags.sh`

- [ ] **Step 1: 先确认生产库关联表名**

在 joho 执行（docker exec psql，库名/容器以环境为准）：

```sql
\dt *tag*lnk*
```
Expected: 存在类似 `activities_zhao_tags_lnk` 或 `zhao_tags_activities_lnk` 的表（manyToMany 关联表）。**表名以实际查询为准，脚本中写死实际表名**。

- [ ] **Step 2: 写迁移脚本（DRY=1 统计 / DRY=0 执行+备份）**

创建 `basic/scripts/migrate_activity_tags.sh`（表名占位按 Step1 实测替换）：

```bash
#!/bin/bash
# 迁移：activity.tags(json) -> zhao_tag 记录 + manyToMany relation + slug 回填
# 用法: DRY=1 bash migrate_activity_tags.sh   # 仅统计
#       DRY=0 bash migrate_activity_tags.sh   # 执行(自动备份)
set -euo pipefail
DRY="${DRY:-1}"
STAMP=$(date +%Y%m%d%H%M%S)
PSQL="docker exec <PG_CONTAINER> psql -U <PG_USER> -d <PG_DB> -tA"
LNK="<LNK_TABLE>" # 实测表名

# 1) 备份 json tags（无论 DRY 与否都先备份，幂等）
$PSQL -c "COPY (SELECT id, title, tags FROM activities WHERE tags IS NOT NULL AND jsonb_typeof(tags)='array') TO STDOUT" > "/home/admin/migrate_activity_tags_${STAMP}.csv" 2>/dev/null || true

# 2) 收集需新建 tag（json 里出现的、zhao_tags 不存在 name 的）
$PSQL -c "
SELECT jsonb_array_elements_text(tags) AS t, count(*) 
FROM activities 
WHERE tags IS NOT NULL AND jsonb_typeof(tags)='array'
GROUP BY t;" > /tmp/act_tag_names.txt

if [ "$DRY" = "1" ]; then
  echo "[DRY] 待处理活动标签名（前 50）:"
  head -50 /tmp/act_tag_names.txt
  echo "[DRY] 完成统计，未做任何写入"
  exit 0
fi

# 3) 新建缺失 tag（name/slug 同值，slug 用 md5 前缀保证唯一）
$PSQL -c "
INSERT INTO zhao_tags (name, slug, is_preset, is_public, created_at, updated_at)
SELECT t.name, 'tag-' || substr(md5(t.name||now()::text),1,8), false, true, now(), now()
FROM (SELECT DISTINCT name FROM /tmp/act_tag_names 语法见下) t
WHERE NOT EXISTS (SELECT 1 FROM zhao_tags z WHERE z.name = t.name)
ON CONFLICT DO NOTHING;"

# 4) 建 relation（逐活动按 json tags 关联 tag）
$PSQL -c "
INSERT INTO $LNK (activity_id, tag_id)
SELECT a.id, z.id
FROM activities a
CROSS JOIN LATERAL jsonb_array_elements_text(a.tags) AS jt
JOIN zhao_tags z ON z.name = jt
WHERE a.tags IS NOT NULL AND jsonb_typeof(a.tags)='array'
  AND NOT EXISTS (SELECT 1 FROM $LNK l WHERE l.activity_id=a.id AND l.tag_id=z.id)
ON CONFLICT DO NOTHING;"

# 5) slug 回填（空 slug 用 documentId 保证唯一稳定）
$PSQL -c "UPDATE activities SET slug = document_id WHERE slug IS NULL OR slug='';"

echo "[OK] 迁移完成，备份: /home/admin/migrate_activity_tags_${STAMP}.csv"
```

> 注意：Step3/4 的 psql 语句中 `CROSS JOIN LATERAL` 直接引用文件不便，改为先建临时表 `/tmp/act_tag_names` 再插入，实现时按下方 Task2 Step3 完整 SQL 落地（此处为骨架）。

- [ ] **Step 3: 迁移脚本落地（完整 SQL 版本）**

完整版核心（含临时表方案）：

```bash
$PSQL -c "DROP TABLE IF EXISTS _act_tags; CREATE TEMP TABLE _act_tags AS SELECT a.id AS activity_id, jt AS tagname FROM activities a, jsonb_array_elements_text(a.tags) jt WHERE a.tags IS NOT NULL AND jsonb_typeof(a.tags)='array';"
$PSQL -c "INSERT INTO zhao_tags (name, slug, is_preset, is_public, created_at, updated_at) SELECT DISTINCT t.tagname, 'tag-'||substr(md5(t.tagname||now()::text),1,8), false, true, now(), now() FROM _act_tags t WHERE NOT EXISTS (SELECT 1 FROM zhao_tags z WHERE z.name=t.tagname) ON CONFLICT DO NOTHING;"
$PSQL -c "INSERT INTO $LNK (activity_id, tag_id) SELECT x.activity_id, z.id FROM _act_tags x JOIN zhao_tags z ON z.name=x.tagname WHERE NOT EXISTS (SELECT 1 FROM $LNK l WHERE l.activity_id=x.activity_id AND l.tag_id=z.id) ON CONFLICT DO NOTHING;"
$PSQL -c "UPDATE activities SET slug = document_id WHERE slug IS NULL OR slug='';"
```

- [ ] **Step 4: 验证**

```sql
-- relation 数 = 原 json tags 总数（去重后）
SELECT count(*) FROM $LNK;
-- 活动 tags 均非空
SELECT count(*) FROM activities a WHERE NOT EXISTS (SELECT 1 FROM $LNK l WHERE l.activity_id=a.id);
```
Expected: relation 数与原标签总量一致；0 条活动无 relation。

- [ ] **Step 5: Commit**

```bash
git add basic/scripts/migrate_activity_tags.sh
git commit -m "feat(zhao-point): activity json tags 迁移脚本(DRY/备份/幂等)"
```

---

### Task 3: zhao-tag 聚合接口

**Files:**
- Create: `basic/plugins/zhao-tag/server/src/services/related.ts`
- Modify: `basic/plugins/zhao-tag/server/src/services/index.ts`
- Create: `basic/plugins/zhao-tag/server/src/controllers/related.ts`
- Modify: `basic/plugins/zhao-tag/server/src/controllers/index.ts`
- Modify: `basic/plugins/zhao-tag/server/src/routes/content-api.ts`

- [ ] **Step 1: 聚合服务**

创建 `services/related.ts`：

```ts
import type { Core } from "@strapi/strapi";

const TYPE_UID: Record<string, string> = {
  article: "plugin::zhao-website.article",
  geoArticle: "plugin::zhao-website.geo-article",
  case: "plugin::zhao-website.case",
  product: "plugin::zhao-website.product",
  faq: "plugin::zhao-website.faq",
  tutorial: "plugin::zhao-website.tutorial",
  course: "plugin::zhao-course.course",
  lesson: "plugin::zhao-course.course-lesson",
  activity: "plugin::zhao-point.activity",
};

const ALL_TYPES = Object.keys(TYPE_UID);

export default ({ strapi }: { strapi: Core.Strapi }) => ({
  async findByTags(tagIds: string[], types: string[], limit = 3, exclude = "") {
    const result: Record<string, any[]> = {};
    for (const type of types) {
      const uid = TYPE_UID[type];
      if (!uid) continue;
      const filters: any = { tags: { documentId: { $in: tagIds } } };
      if (exclude && type === "activity") filters.documentId = { $ne: exclude };
      const docs: any[] = await strapi.documents(uid).findMany({
        filters,
        limit,
        sort: { updatedAt: "desc" },
        fields: ["title", "slug", "excerpt", "description", "updatedAt"],
      });
      result[type] = docs.map((d) => ({
        type,
        documentId: d.documentId,
        title: d.title ?? "",
        summary: d.excerpt || d.description || "",
        url: this.buildUrl(type, d),
      }));
    }
    return result;
  },

  buildUrl(type: string, d: any) {
    switch (type) {
      case "geoArticle":
        return d.slug ? `https://www.joho.cn/geo-article/${d.slug}` : "";
      case "course":
        return `https://v.joho.cn/pages/course-detail/course-detail?id=${d.documentId}`;
      case "activity":
        return `https://v.joho.cn/pages/activity/detail?id=${d.documentId}`;
      default:
        return ""; // article/case/product/faq/tutorial/lesson 暂无落地页
    }
  },

  defaultTypes() {
    return ALL_TYPES;
  },
});
```

- [ ] **Step 2: 注册 service**

修改 `services/index.ts`，加入 `related: createCoreService` 或按现有惯例：

```ts
import related from "./related";
// ...
export default {
  related,
  // ...既有服务
};
```

（以现有 `services/index.ts` 结构为准，保持键名 `related`。）

- [ ] **Step 3: 控制器**

创建 `controllers/related.ts`：

```ts
import type { Core } from "@strapi/strapi";

const wrapList = (data: any, meta: any = {}) => ({ data, meta });

export default ({ strapi }: { strapi: Core.Strapi }) => ({
  async findByTags(ctx: any) {
    try {
      const { tags, types, limit = "3", exclude = "" } = ctx.query;
      if (!tags) { ctx.status = 400; ctx.body = { error: "tags 必填（逗号分隔 tag documentId）" }; return; }
      const tagIds = String(tags).split(",").map((s: string) => s.trim()).filter(Boolean);
      const svc = strapi.plugin("zhao-tag").service("related");
      const typeList = types ? String(types).split(",").map((s: string) => s.trim()).filter(Boolean) : svc.defaultTypes();
      const numLimit = Math.max(1, Math.min(20, parseInt(String(limit), 10) || 3));
      ctx.body = wrapList(await svc.findByTags(tagIds, typeList, numLimit, String(exclude)));
    } catch (err) {
      ctx.status = (err as any).status || 400;
      ctx.body = { error: (err as Error).message };
    }
  },
});
```

- [ ] **Step 4: 注册 controller + 路由**

`controllers/index.ts` 加 `related` 导出；`routes/content-api.ts` 在公开路由区追加：

```ts
publicRoute("GET", "/related-by-tags", "related.findByTags"),
```

- [ ] **Step 5: 本地验证**

本地 Strapi（含本地库有活动+标签数据）验证：

```bash
curl "http://localhost:1337/api/zhao-tag/v1/related-by-tags?tags=<tagDocumentId>&limit=2"
```
Expected: 200，`data` 为 `{ article: [...], case: [], ... }` 各类型数组。无 tags 时 400。

- [ ] **Step 6: Commit（含 dist 重建）**

```bash
npm run build   # 在 basic/plugins/zhao-tag/
git add basic/plugins/zhao-tag/server/src/services/related.ts basic/plugins/zhao-tag/server/src/controllers/related.ts basic/plugins/zhao-tag/server/src/routes/content-api.ts basic/plugins/zhao-tag/server/src/services/index.ts basic/plugins/zhao-tag/server/src/controllers/index.ts basic/plugins/zhao-tag/dist
git commit -m "feat(zhao-tag): 新增 related-by-tags 聚合接口"
```

---

### Task 4: 管理端活动编辑页改造

**Files:**
- Modify: `web/src/pages/activity/form.vue`

- [ ] **Step 1: tags 改用 TagSelector**

按 article/edit.vue 模式：import `TagSelector`，模板 `<TagSelector v-model="form.tags" :siteId="siteId" label="标签" />`；`form.tags` 初始 `[]`、提交 `tags: Array.isArray(form.tags) && form.tags.length ? form.tags.map(t => t.documentId || t) : undefined` 逻辑按后端 relation 提交（documentId 数组）。

> 提交格式与 zhao-website 其他 CT 对齐（查看 article/edit.vue 的 tags 提交格式并保持一致）。

- [ ] **Step 2: 新增"相关内容配置"折叠区**

在表单底部（报名相关区之后）追加 `relatedOverride` 配置：7 类多选（文章/案例/产品/FAQ/课程/教程/活动），每类从对应 list API 拉取，`documentId` 数组存 `form.relatedOverride`；提交时 `relatedOverride: form.relatedOverride`。活动级开关 `showRelatedSection` 用 switch。

- [ ] **Step 3: 构建验证**

```bash
# web 工程构建（HBuilder X CLI 或本地 npm run build:h5，以工程现状为准）
```
Expected: 构建通过，无编译错误。

- [ ] **Step 4: Commit**

```bash
git add web/src/pages/activity/form.vue
git commit -m "feat(web): 活动编辑页接入标签选择与相关内容配置"
```

---

### Task 5: 部署与验证

- [ ] **Step 1: dist 已含全部新接口（自检）**

```bash
grep -r "related-by-tags" basic/plugins/zhao-tag/dist/server --include=*.js
grep -r "pointActivities" basic/plugins/zhao-point/dist/server --include=*.js
```
Expected: 均有命中（未命中=未重建，必须先 build 再提交）。

- [ ] **Step 2: 部署（走 deploy.sh，禁止裸手命令）**

```bash
# 按项目既有部署流程执行（basic 仓库 → joho，含重启 strapi）
```

- [ ] **Step 3: 执行迁移（先 DRY）**

```bash
ssh joho "DRY=1 bash /tmp/migrate_activity_tags.sh"   # 统计
ssh joho "DRY=0 bash /tmp/migrate_activity_tags.sh"   # 执行+备份
```

- [ ] **Step 4: 线上 curl 验证**

```bash
curl "https://www.joho.cn/api/zhao-tag/v1/related-by-tags?tags=<线上tagId>&limit=2"
curl "https://www.joho.cn/api/zhao-point/v1/activities?pageSize=1"
```
Expected: 200；activities 返回项含 `tags`（relation 数组，documentId）；未登录访问聚合接口 200（公开）。

---

## Self-Review

**1. Spec coverage:**
- §3.1 activity.tags relation 对齐 → Task1 ✓
- §3.1 数据迁移 → Task2 ✓
- §3.2 slug/relatedOverride/showRelatedSection → Task1+Task2+Task4 ✓
- §4 聚合接口（含权限 public、URL 规则）→ Task3 ✓
- 管理端 TagSelector → Task4 ✓

**2. Placeholder scan:** 迁移脚本表名/容器名/用户名为 `<...>` 占位，需按环境实测替换（Step1 明示）；services/index.ts 注册以现有结构为准（Task3 Step2 标注）。其余无 TBD。

**3. Type consistency:** `findByTags(tagIds, types, limit, exclude)` 在 service/controller 一致；类型键 `geoArticle` 在 service 内定义并用于 URL 规则；route handler `related.findByTags` 与 controller 方法名一致。
