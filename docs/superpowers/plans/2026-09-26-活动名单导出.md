# 活动名单导出 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 管理端活动报名名单页一键导出「全量名单 + 到场状态」CSV（含报名表单自定义字段），供现场点名与事后核对爽约。

**Architecture:** 后端新增一个 `channelScopeRoute` 导出接口，复用 `activity.read` 权限点；数据组装（报名 × 到场记录 join、状态推导）放在**不依赖 strapi 的纯模块** `signup-export.ts` 里以便单测，控制器只做查询与归一。前端复用既有 `downloadFile()`（补一个 MIME 参数）触发浏览器下载。

**Tech Stack:** Strapi 5 插件（TypeScript，CommonJS 构建产物在 `dist/`）、jest + ts-jest、Vue3 + uni-app H5（web 管理端）。

**上游设计：** [2026-09-26-活动名单导出-design.md](../specs/2026-09-26-活动名单导出-design.md)

---

## 文件结构

| 动作 | 文件 | 职责 |
|---|---|---|
| Create | `basic/plugins/zhao-point/server/src/services/signup-export.ts` | 纯逻辑：单元格转义、UTC+8 时间格式化、到场状态推导、CSV 组装（无 strapi 依赖，可单测） |
| Create | `basic/plugins/zhao-point/tests/signup-export.test.ts` | 上述纯逻辑的 jest 单测（含 `{id:N}` 关系对象形态覆盖） |
| Modify | `basic/plugins/zhao-point/server/src/controllers/activity.ts`（`adminSignups` 之后） | 新增 `adminExportSignups` 控制器 |
| Modify | `basic/plugins/zhao-point/server/src/routes/content-api.ts:192` 之后 | 新增导出路由 |
| Modify | `web/src/utils/request.js:253-319` | `downloadFile` 增加第 4 参 `mime`（默认值保持 xlsx，现有调用零影响） |
| Modify | `web/src/api/activity.js`（`getActivitySignups` 附近） | `exportSignupListCsv(documentId, filename)` |
| Modify | `web/src/pages/activity/signups.vue` | 报名名单 tab 加导出按钮 + `activityTitle` |

**不涉及**：`shao`（C 端）、数据模型（无新表/新字段）、权限点（复用 `activity.read`）。

---

### Task 1: 导出纯逻辑 + 单测

**Files:**
- Create: `e:\code\basic\plugins\zhao-point\server\src\services\signup-export.ts`
- Test: `e:\code\basic\plugins\zhao-point\tests\signup-export.test.ts`

- [ ] **Step 1: 写失败测试**

创建 `e:\code\basic\plugins\zhao-point\tests\signup-export.test.ts`：

```ts
import {
  CSV_BOM,
  csvCell,
  formatCell,
  formatDateTimeCst,
  attendanceStatus,
  buildSignupCsv,
} from '../server/src/services/signup-export';

describe('活动名单导出纯逻辑', () => {
  test('CSV_BOM 是 UTF-8 BOM', () => {
    expect(CSV_BOM).toBe('\uFEFF');
    expect(CSV_BOM.charCodeAt(0)).toBe(0xfeff);
  });

  test('formatCell：数组用「、」连接，空值转空串', () => {
    expect(formatCell(null)).toBe('');
    expect(formatCell(undefined)).toBe('');
    expect(formatCell('')).toBe('');
    expect(formatCell(0)).toBe('0');
    expect(formatCell(['茶道', '香道'])).toBe('茶道、香道');
    expect(formatCell(['茶道', '', '香道'])).toBe('茶道、香道');
    expect(formatCell([])).toBe('');
  });

  test('csvCell：统一双引号包裹，内部引号翻倍', () => {
    expect(csvCell('张三')).toBe('"张三"');
    expect(csvCell('含,逗号')).toBe('"含,逗号"');
    expect(csvCell('含"引号"')).toBe('"含""引号"""');
    expect(csvCell(null)).toBe('""');
    expect(csvCell(12)).toBe('"12"');
    expect(csvCell(['a', 'b'])).toBe('"a、b"');
  });

  test('formatDateTimeCst：按 UTC+8 输出 YYYY-MM-DD HH:mm', () => {
    // 2026-09-26T01:30:00Z → 北京时间 09:30
    expect(formatDateTimeCst('2026-09-26T01:30:00.000Z')).toBe('2026-09-26 09:30');
    // 跨日：UTC 20:00 → 次日 04:00
    expect(formatDateTimeCst('2026-09-25T20:00:00.000Z')).toBe('2026-09-26 04:00');
    expect(formatDateTimeCst(new Date('2026-09-26T01:30:00.000Z'))).toBe('2026-09-26 09:30');
    expect(formatDateTimeCst(null)).toBe('');
    expect(formatDateTimeCst('')).toBe('');
    expect(formatDateTimeCst('不是时间')).toBe('');
  });

  test('到场状态推导：已取消 > 已到场 > 未到场', () => {
    const att = { method: 'worker_scan', checkinAt: '2026-09-26T01:30:00.000Z' };
    expect(attendanceStatus({ id: 1, status: 'active' }, undefined)).toBe('未到场');
    expect(attendanceStatus({ id: 1, status: 'active' }, att)).toBe('已到场');
    expect(attendanceStatus({ id: 1, status: 'waiting' }, undefined)).toBe('未到场');
    expect(attendanceStatus({ id: 1, status: 'waiting' }, att)).toBe('已到场');
    // 取消优先：即使有到场记录也显示已取消
    expect(attendanceStatus({ id: 1, status: 'cancelled' }, att)).toBe('已取消');
    expect(attendanceStatus({ id: 1, status: 'cancelled' }, undefined)).toBe('已取消');
  });

  test('buildSignupCsv：表头 = 9 固定列 + 动态表单列，带 BOM 与 CRLF', () => {
    const csv = buildSignupCsv({
      formFields: [{ key: 'name', label: '姓名' }, { key: 'phone', label: '手机号' }],
      signups: [
        {
          id: 11,
          status: 'active',
          pointsCharged: 50,
          signupAt: '2026-09-26T01:30:00.000Z',
          formData: { name: '张三', phone: '13800000000' },
          user: { id: 7, nickname: '小张' },
        },
      ],
      attendanceBySignupId: { '11': { method: 'manual', checkinAt: '2026-09-26T02:00:00.000Z' } },
    });

    expect(csv.startsWith(CSV_BOM)).toBe(true);
    const body = csv.slice(1);
    const lines = body.split('\r\n');
    expect(lines[0]).toBe(
      '"序号","用户ID","昵称","报名状态","到场状态","核销方式","报名时间","到场时间","扣除积分","姓名","手机号"',
    );
    expect(lines[1]).toBe(
      '"1","7","小张","已报名","已到场","手动核销","2026-09-26 09:30","2026-09-26 10:00","50","张三","13800000000"',
    );
    expect(lines[2]).toBe(''); // 末尾 CRLF
  });

  test('buildSignupCsv：无到场记录=未到场、核销方式与到场时间为空；缺字段留空', () => {
    const csv = buildSignupCsv({
      formFields: [{ key: 'name', label: '姓名' }],
      signups: [
        { id: 1, status: 'waiting', pointsCharged: null, signupAt: null, formData: null, user: { id: 3, username: 'wx_3' } },
      ],
      attendanceBySignupId: null,
    });
    const lines = csv.slice(1).split('\r\n');
    expect(lines[1]).toBe('"1","3","wx_3","候补中","未到场","","","","0",""');
  });

  test('buildSignupCsv：昵称缺失回退用户#id；取消报名显示已取消', () => {
    const csv = buildSignupCsv({
      signups: [{ id: 5, status: 'cancelled', pointsCharged: 0, formData: {}, user: { id: 9 } }],
    });
    const lines = csv.slice(1).split('\r\n');
    expect(lines[1]).toBe('"1","9","用户#9","已取消","已取消","","","","0"');
  });

  test('buildSignupCsv：多行/逗号/引号取值不破坏列结构', () => {
    const csv = buildSignupCsv({
      formFields: [{ key: 'note', label: '备注' }],
      signups: [
        {
          id: 1,
          status: 'active',
          signupAt: '2026-09-26T01:30:00.000Z',
          formData: { note: '第一行\n第二行,带逗号"带引号"' },
          user: { id: 2, nickname: '甲' },
        },
      ],
    });
    const body = csv.slice(1);
    // 表头 + 1 数据行的「逻辑」行；换行被引号包裹，故按 CRLF 切只有 2 段 + 末尾空段
    const lines = body.split('\r\n');
    expect(lines.length).toBe(3);
    expect(lines[1]).toContain('"第一行\n第二行,带逗号""带引号"""');
  });

  test('buildSignupCsv：空名单只输出表头', () => {
    const csv = buildSignupCsv({ signups: [], formFields: [] });
    expect(csv).toBe(`${CSV_BOM}"序号","用户ID","昵称","报名状态","到场状态","核销方式","报名时间","到场时间","扣除积分"\r\n`);
  });

  test('buildSignupCsv：关系字段以 {id:N} 对象形态入参也能正确归位（防 NaN 透传）', () => {
    // 模拟 strapi 查询结果：signup.user 为对象、attendanceBySignupId 的键为字符串化 id
    const csv = buildSignupCsv({
      formFields: [],
      signups: [
        { id: 21, status: 'active', formData: {}, user: { id: 8, nickname: '乙' } },
        { id: 22, status: 'active', formData: {}, user: null },
      ],
      attendanceBySignupId: { '22': { method: 'self', checkinAt: '2026-09-26T02:00:00.000Z' } },
    });
    const lines = csv.slice(1).split('\r\n');
    expect(lines[1]).toBe('"1","8","乙","已报名","未到场","","","","0"');
    expect(lines[2]).toBe('"2","","","已报名","已到场","自助核销","","2026-09-26 10:00","0"');
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run（在 `e:\code\basic\plugins\zhao-point`）：
```
npx jest tests/signup-export.test.ts
```
Expected: FAIL — `Cannot find module '../server/src/services/signup-export'`

- [ ] **Step 3: 写最小实现**

创建 `e:\code\basic\plugins\zhao-point\server\src\services\signup-export.ts`：

```ts
/**
 * 活动名单导出的纯逻辑（不依赖 strapi，便于单测）。
 * 入参为已归一化的报名 / 到场数据，输出 UTF-8 BOM + CRLF 的 CSV 文本。
 */

/** UTF-8 BOM：Excel 打开中文表头不乱码 */
export const CSV_BOM = "\uFEFF";

/** 报名状态 → 中文 */
const SIGNUP_STATUS_TEXT: Record<string, string> = {
  active: "已报名",
  waiting: "候补中",
  cancelled: "已取消",
};

/** 核销方式 → 中文 */
const METHOD_TEXT: Record<string, string> = {
  self: "自助核销",
  worker_scan: "工作人员扫码",
  manual: "手动核销",
};

const FIXED_HEADERS = [
  "序号", "用户ID", "昵称", "报名状态", "到场状态", "核销方式", "报名时间", "到场时间", "扣除积分",
];

/** 固定列数量（动态表单列从该下标之后开始） */
export const FIXED_COLUMN_COUNT = FIXED_HEADERS.length;

/** 单元格值格式化：数组用「、」连接；null/undefined/空串 → "" */
export function formatCell(value: unknown): string {
  if (value === null || value === undefined) return "";
  if (Array.isArray(value)) {
    return value.map((v) => formatCell(v)).filter((s) => s !== "").join("、");
  }
  return String(value);
}

/** 单格 CSV 编码：统一双引号包裹，内部引号翻倍 */
export function csvCell(value: unknown): string {
  return `"${formatCell(value).replace(/"/g, '""')}"`;
}

/**
 * 按 UTC+8 格式化时间为 "YYYY-MM-DD HH:mm"。
 * 固定偏移而非依赖服务器时区，避免线上 UTC 环境整列偏 8 小时；非法/空值返回 ""。
 */
export function formatDateTimeCst(value: unknown): string {
  if (value === null || value === undefined || value === "") return "";
  const d = value instanceof Date ? value : new Date(value as any);
  if (Number.isNaN(d.getTime())) return "";
  const t = new Date(d.getTime() + 8 * 60 * 60 * 1000);
  const p = (n: number) => String(n).padStart(2, "0");
  return `${t.getUTCFullYear()}-${p(t.getUTCMonth() + 1)}-${p(t.getUTCDate())} ${p(t.getUTCHours())}:${p(t.getUTCMinutes())}`;
}

export type ExportFormField = { key: string; label: string };

export type ExportSignup = {
  id: number | string;
  status?: string | null;
  pointsCharged?: number | string | null;
  signupAt?: unknown;
  formData?: Record<string, unknown> | null;
  user?: { id?: number | string | null; nickname?: string | null; username?: string | null } | null;
};

export type ExportAttendance = { method?: string | null; checkinAt?: unknown };

/**
 * 到场状态推导（本特性核心规则）：已取消 > 已到场 > 未到场。
 * 「未到场」= 报名仍在册（含候补）但无到场记录，即运营口径的爽约集合。
 */
export function attendanceStatus(signup: ExportSignup, attendance?: ExportAttendance | null): string {
  if (signup?.status === "cancelled") return "已取消";
  return attendance ? "已到场" : "未到场";
}

/**
 * 组装名单 CSV 文本（含 BOM 与 CRLF）。
 * 按 signups 入参顺序输出（调用方保证已按报名时间升序），一行一人。
 */
export function buildSignupCsv(input: {
  formFields?: ExportFormField[] | null;
  signups?: ExportSignup[] | null;
  attendanceBySignupId?: Record<string, ExportAttendance> | null;
}): string {
  const fields = (Array.isArray(input.formFields) ? input.formFields : []).filter((f) => f && f.key);
  const signups = Array.isArray(input.signups) ? input.signups : [];
  const attMap = input.attendanceBySignupId || {};

  const headers = [...FIXED_HEADERS, ...fields.map((f) => f.label || f.key)];
  const lines = [headers.map(csvCell).join(",")];

  signups.forEach((s, i) => {
    const att = attMap[String(s.id)];
    const userId = s.user?.id ?? "";
    const nick = s.user?.nickname || s.user?.username || (userId !== "" ? `用户#${userId}` : "");
    const row: unknown[] = [
      i + 1,
      userId,
      nick,
      SIGNUP_STATUS_TEXT[String(s.status)] ?? formatCell(s.status),
      attendanceStatus(s, att),
      att ? METHOD_TEXT[String(att.method)] ?? formatCell(att.method) : "",
      formatDateTimeCst(s.signupAt),
      formatDateTimeCst(att?.checkinAt),
      s.pointsCharged ?? 0,
    ];
    for (const f of fields) row.push(formatCell(s.formData?.[f.key]));
    lines.push(row.map(csvCell).join(","));
  });

  return `${CSV_BOM}${lines.join("\r\n")}\r\n`;
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npx jest tests/signup-export.test.ts`
Expected: PASS，11 个 test 全绿。

- [ ] **Step 5: 提交**

```powershell
git -C e:\code\basic add plugins/zhao-point/server/src/services/signup-export.ts plugins/zhao-point/tests/signup-export.test.ts
git -C e:\code\basic commit -m "feat(activity): 活动名单导出纯逻辑与单测"
```

---

### Task 2: 控制器与路由

**Files:**
- Modify: `e:\code\basic\plugins\zhao-point\server\src\controllers\activity.ts`（1-13 行 import；`adminSignups` 之后）
- Modify: `e:\code\basic\plugins\zhao-point\server\src\routes\content-api.ts:192` 之后

- [ ] **Step 1: 加 import**

在 `e:\code\basic\plugins\zhao-point\server\src\controllers\activity.ts` 顶部，`import { decodeScanText, validateManualReason } from "../services/checkin-ticket";`（第 5 行）之后新增：

```ts
import { buildSignupCsv } from "../services/signup-export";
```

- [ ] **Step 2: 新增 `adminExportSignups` 控制器**

在 `e:\code\basic\plugins\zhao-point\server\src\controllers\activity.ts` 的 `adminSignups`（结束于第 671 行 `},`）**之后**、`adminCancelSignup`（第 673 行）**之前**插入：

```ts
  // GET /adm/activities/:documentId/signups/export  名单导出（CSV，含到场状态）
  // 一行一人；到场状态由到场记录推导（无记录=未到场，取消报名=已取消），不改数据模型。
  async adminExportSignups(ctx: any) {
    try {
      const act = await strapi.documents(ACTIVITY_UID).findOne({ documentId: ctx.params.documentId });
      if (!act) { ctx.status = 404; ctx.body = { error: "活动不存在" }; return; }

      const signups = await strapi.db.query(SIGNS_UID).findMany({
        where: { activity: act.id },
        populate: { user: true },
        orderBy: { signupAt: "asc" },
      });

      // 到场记录按 signup id 建索引：关系字段统一走 relId() 归一，NaN 直接跳过（禁止透传给下游）
      const attRows = await strapi.db.query(ATT_UID).findMany({ populate: { signup: true } });
      const attendanceBySignupId: Record<string, any> = {};
      for (const a of attRows) {
        const sid = relId(a.signup);
        if (!Number.isFinite(sid)) continue;
        attendanceBySignupId[String(sid)] = { method: a.method, checkinAt: a.checkinAt };
      }

      const actFormConfig: any = (act as any).formConfig;
      const csv = buildSignupCsv({
        formFields: (Array.isArray(actFormConfig) ? actFormConfig : [])
          .filter((f: any) => f && f.key)
          .map((f: any) => ({ key: String(f.key), label: String(f.label || f.key) })),
        signups: signups as any,
        attendanceBySignupId,
      });

      // 文件名：ASCII 兜底 + RFC 5987 中文（前端 downloadFile 会覆盖保存名）
      const stamp = new Date(Date.now() + 8 * 3600 * 1000).toISOString().slice(0, 10).replace(/-/g, "");
      const named = encodeURIComponent(`活动名单_${(act as any).title || "活动"}_${stamp}.csv`);
      ctx.set("Content-Type", "text/csv; charset=utf-8");
      ctx.set("Content-Disposition", `attachment; filename="signups.csv"; filename*=UTF-8''${named}`);
      ctx.body = csv;
    } catch (e: any) {
      ctx.status = (e as any).status || 400;
      ctx.body = { error: e.message };
    }
  },
```

> 说明：`relId()` 是本文件内部（第 75 行）已定义的归一函数，直接复用，不要新写一份。

- [ ] **Step 3: 注册路由**

在 `e:\code\basic\plugins\zhao-point\server\src\routes\content-api.ts` 中，`channelScopeRoute("GET", "/adm/activities/:documentId/signups", "activity.adminSignups", "activity.read"),`（第 192 行）**之后**新增一行：

```ts
    channelScopeRoute("GET", "/adm/activities/:documentId/signups/export", "activity.adminExportSignups", "activity.read"),
```

- [ ] **Step 4: 跑全量测试确认无回归**

Run（在 `e:\code\basic\plugins\zhao-point`）：`npx jest`
Expected: 全部 PASS（含 Task 1 的 11 个）。

- [ ] **Step 5: 提交**

```powershell
git -C e:\code\basic add plugins/zhao-point/server/src/controllers/activity.ts plugins/zhao-point/server/src/routes/content-api.ts
git -C e:\code\basic commit -m "feat(activity): 新增活动名单 CSV 导出接口"
```

---

### Task 3: 重建 dist、部署自检、本地冒烟

**Files:**
- Modify（生成物）：`e:\code\basic\plugins\zhao-point\dist/**`

- [ ] **Step 1: 本地重建 dist**

Run（在 `e:\code\basic\plugins\zhao-point`）：`npm run build`
Expected: 构建成功，无 TS 报错。

- [ ] **Step 2: 部署自检（强制，防 dist 静默失效）**

Run：
```
Select-String -Path "e:\code\basic\plugins\zhao-point\dist\server\index.js" -Pattern "adminExportSignups" | Select-Object -First 2
Select-String -Path "e:\code\basic\plugins\zhao-point\dist\server\index.js" -Pattern "signups/export" | Select-Object -First 2
Select-String -Path "e:\code\basic\plugins\zhao-point\dist\server\index.js" -Pattern "attendanceStatus|未到场" | Select-Object -First 2
```
Expected: 三处均有命中。无命中 = dist 未重建，必须重跑 `npm run build` 后重提。

- [ ] **Step 3: 本地冒烟（本地 Strapi 1337）**

先取管理端 JWT：
```
curl.exe -s -X POST "http://localhost:1337/api/zhao-auth/v1/admin/auth/local" -H "Content-Type: application/json" -d "{\"identifier\":\"zhao\",\"password\":\"a963963\"}"
```
取任一活动 documentId（`GET /api/zhao-point/v1/activities`），然后：

1) 未带 token → 必须 401/403（**不是 404**，404 说明路由未注册）：
```
curl.exe -s -o NUL -w "%{http_code}" "http://localhost:1337/api/zhao-point/v1/admin/adm/activities/<活动documentId>/signups/export"
```
2) 带 token → 200 + CSV 头 + BOM（`EF BB BF`）：
```
curl.exe -s -D - -o export.csv "http://localhost:1337/api/zhao-point/v1/admin/adm/activities/<活动documentId>/signups/export" -H "Authorization: Bearer <JWT>"
```
Expected: `Content-Type: text/csv; charset=utf-8`；`Format-Hex export.csv | Select-Object -First 1` 首三字节为 `EF BB BF`；用 Excel 或 `Get-Content export.csv -Encoding UTF8` 检查表头 9 列 + 动态列、到场状态取值合理。

> 若本地 Strapi 未运行，记录此步为「待生产验收等价覆盖」，不得跳过生产验收。

- [ ] **Step 4: 提交 dist 并推送 basic**

```powershell
git -C e:\code\basic add plugins/zhao-point/dist
git -C e:\code\basic commit -m "build(activity): 重建 zhao-point dist（名单导出）"
git -C e:\code\basic push origin main
```

---

### Task 4: 发布 basic 到生产并验收接口

**Files:** 无代码改动

- [ ] **Step 1: 确认无冲突**

```powershell
ssh joho "cd /www/apps/strapi && git fetch origin main && git diff --name-only HEAD origin/main"
```
Expected: 只列出本次改动的 `plugins/zhao-point/**` 与 `docs/**`；若出现 `ecosystem.config.cjs` / `package-lock.json` / `deploy-zhao-sso.sh` 等服务器本地改动，**停下询问**。

- [ ] **Step 2: ff-only 合并并重启（不走 deploy.sh，避免 2G 机 OOM）**

```powershell
ssh joho "cd /www/apps/strapi && git merge --ff-only origin/main"
ssh joho "export PATH=/home/admin/.nvm/versions/node/v22.23.1/bin:/usr/bin:/bin:`$PATH; export PM2_HOME=/home/admin/.pm2; pm2 restart strapi"
```

- [ ] **Step 3: 等待就绪**

重启后约 60s 才监听端口，轮询（000 属正常）：
```powershell
curl.exe -s -o NUL -w "%{http_code}" "https://joho.cn/api/zhao-point/v1/activities"
```
Expected: 反复探测直至 200。

- [ ] **Step 4: 生产验收（在服务器内 curl，本机访问域名不可用）**

用 base64 传脚本（Windows ssh 传含引号命令一律 base64）：
```powershell
$script = @'
set -e
JWT=$(curl -s -X POST "http://127.0.0.1:1337/api/zhao-auth/v1/admin/auth/local" -H "Content-Type: application/json" -d '{"identifier":"zhao","password":"a963963"}' | sed -n 's/.*"jwt":"\([^"]*\)".*/\1/p')
echo "JWT_LEN=${#JWT}"
DID=$(curl -s "http://127.0.0.1:1337/api/zhao-point/v1/activities?pageSize=1" | sed -n 's/.*"documentId":"\([^"]*\)".*/\1/p' | head -1)
echo "DID=$DID"
echo "--- 未带 token ---"
curl -s -o /dev/null -w "%{http_code}\n" "http://127.0.0.1:1337/api/zhao-point/v1/admin/adm/activities/$DID/signups/export"
echo "--- 带 token 响应头 ---"
curl -s -D - -o /tmp/signups_export.csv "http://127.0.0.1:1337/api/zhao-point/v1/admin/adm/activities/$DID/signups/export" -H "Authorization: Bearer $JWT" | head -6
echo "--- BOM 前三字节（应为 efbbbf）---"
head -c 3 /tmp/signups_export.csv | xxd
echo "--- 表头 ---"
sed -n '1p' /tmp/signups_export.csv
'@
$b64 = [Convert]::ToBase64String([Text.Encoding]::UTF8.GetBytes($script))
ssh joho "echo $b64 | base64 -d | bash"
```
Expected：
- 未带 token → `401`（或 403），**不是 404**；
- 带 token → `Content-Type: text/csv; charset=utf-8`、`HTTP/1.1 200`；
- BOM 为 `efbbbf`；
- 表头含 `"序号","用户ID","昵称","报名状态","到场状态","核销方式","报名时间","到场时间","扣除积分"`。

- [ ] **Step 5: 用真实活动做数据核对（关键验收）**

在 DB 里挑一个**既有报名又有到场记录**的活动，导出后核对：同一 signup 出现在一行；有到场记录者显示 `已到场` 且到场时间/核销方式非空；无记录者显示 `未到场`；`cancelled` 报名显示 `已取消`。
```powershell
ssh joho "docker exec -i 1Panel-postgresql-pIe0 psql -U youshaop -d strapi -c \"SELECT s.id, s.status, s.attended_at IS NOT NULL AS has_att, a.checkin_at FROM activity_signups s LEFT JOIN activity_attendances a ON a.signup_id = s.id ORDER BY s.id DESC LIMIT 10;\""
```
与导出的同名活动行逐条比对，**数量与状态必须完全一致**（不一致则停下排查 relId/join，不得带病上线）。

---

### Task 5: web 管理端导出按钮

**Files:**
- Modify: `e:\code\web\src\utils\request.js:253-319`
- Modify: `e:\code\web\src\api\activity.js`（`getActivitySignups` 附近）
- Modify: `e:\code\web\src\pages\activity\signups.vue`

- [ ] **Step 1: `downloadFile` 支持自定义 MIME**

把 `e:\code\web\src\utils\request.js` 第 253 行：

```js
export function downloadFile(url, params = {}, filename = 'download') {
```
改为：

```js
export function downloadFile(url, params = {}, filename = 'download', mime = 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet') {
```

并把 `triggerBrowserDownload(res.data, filename)`（第 272 行）改为：

```js
            const saved = triggerBrowserDownload(res.data, filename, mime)
```

把 `function triggerBrowserDownload(arrayBuffer, filename)`（第 302 行）改为：

```js
function triggerBrowserDownload(arrayBuffer, filename, mime) {
```
并把其中 `const blob = new Blob([arrayBuffer], {` 之后的 `type:` 行改为：

```js
    type: mime || 'application/octet-stream'
```

> 现有调用方（`api/quiz.js` 的 `downloadQuizTemplate`）不传第 4 参，走默认 xlsx，行为不变。

- [ ] **Step 2: 新增 API 封装**

在 `e:\code\web\src\api\activity.js` 中，`getActivitySignups`（第 53-56 行）**之后**新增：

```js
// 导出报名名单 CSV（全量名单 + 到场状态 + 报名表单自定义字段；后端直出 text/csv）
export function exportSignupListCsv(documentId, filename = '活动名单.csv') {
  return downloadFile(`${ADMIN}/activities/${documentId}/signups/export`, {}, filename, 'text/csv;charset=utf-8')
}
```
并把文件顶部 import（第 1 行）改为：

```js
import { get, post, put, del, downloadFile } from '../utils/request.js'
```

- [ ] **Step 3: 页面加导出按钮**

在 `e:\code\web\src\pages\activity\signups.vue` 中，把报名名单 tab 的容器前（第 10 行 `<!-- 报名名单 -->` 之后）新增工具栏：

```html
    <!-- 报名名单 -->
    <view v-if="activeTab === 'signup'" class="toolbar">
      <view class="export-btn" :class="{ disabled: exporting }" @click="handleExport">
        <text>{{ exporting ? '导出中...' : '导出名单（CSV）' }}</text>
      </view>
    </view>
```

并把原本的 `<view v-if="activeTab === 'signup'" class="list">` 保留不动（工具栏与其并列，同条件渲染）。

- [ ] **Step 4: 脚本补 ref 与处理函数**

把 `import { getActivitySignups, getActivityAttendance, cancelActivitySignup, getActivity } from '../../api/activity.js'` 改为：

```js
import { getActivitySignups, getActivityAttendance, cancelActivitySignup, getActivity, exportSignupListCsv } from '../../api/activity.js'
```

在 `const formConfig = ref([])`（第 85 行）之后新增：

```js
const activityTitle = ref('')
const exporting = ref(false)
```

把 `loadFormConfig` 中的：

```js
    formConfig.value = Array.isArray(act?.formConfig) ? act.formConfig : []
```
改为：

```js
    formConfig.value = Array.isArray(act?.formConfig) ? act.formConfig : []
    activityTitle.value = act?.title || ''
```

在 `removeWaiting` 之前新增：

```js
// 导出名单 CSV（全量名单 + 到场状态）。downloadFile 内部已给出失败提示，此处不重复弹
async function handleExport() {
  if (!activityId.value || exporting.value) return
  exporting.value = true
  uni.showLoading({ title: '导出中...' })
  try {
    // 文件名去掉 Windows 非法字符
    const safeTitle = String(activityTitle.value || '活动').replace(/[\\/:*?"<>|]/g, '')
    await exportSignupListCsv(activityId.value, `活动名单_${safeTitle}.csv`)
    uni.hideLoading()
    uni.showToast({ title: '已保存到浏览器下载目录', icon: 'success' })
  } catch (e) {
    uni.hideLoading()
  } finally {
    exporting.value = false
  }
}
```

- [ ] **Step 5: 样式**

在 `<style scoped>` 的 `.tabs` 规则之后新增：

```css
.toolbar { display: flex; justify-content: flex-end; margin-bottom: 16rpx; }
.export-btn { background: #fff; border: 1rpx solid #667eea; color: #667eea; font-size: 26rpx; padding: 10rpx 24rpx; border-radius: 8rpx; }
.export-btn.disabled { opacity: 0.6; }
```

- [ ] **Step 6: 构建**

Run（在 `e:\code\web`）：`npm run build:h5`
Expected: 构建成功。

- [ ] **Step 7: 提交并推送**

```powershell
git -C e:\code\web add src/utils/request.js src/api/activity.js src/pages/activity/signups.vue
git -C e:\code\web commit -m "feat(activity): 报名名单页新增导出 CSV 按钮"
git -C e:\code\web push origin main
```
> 严禁 `git add -A`：仓库内有未提交的 `src/pages/activity/form.vue` 与大量无关未跟踪文件，**不得**混入。

- [ ] **Step 8: 部署 web 构建产物**

```powershell
cd e:\code\web; .\deploy-h5.ps1
```
Expected: 部署完成，目标 `h.joho.cn`。

---

### Task 6: 线上端到端验收与记忆回填

**Files:**
- Modify: `e:\code\basic\docs\superpowers\specs\2026-09-26-活动名单导出-design.md`（追加「上线与验收记录」小节）
- Modify: `c:\Users\Administrator\.trae-cn\memory\projects\-e-code--p2-3a5c4f0315cfc1daa3fd\project_memory.md`（工程惯例段补一条）

- [ ] **Step 1: 线上端到端**

1. `h.joho.cn` → 活动管理 → 某活动 → 报名名单 → 「导出名单（CSV）」→ 浏览器保存文件。
2. Excel 打开：无乱码；列数 = 9 + 该活动表单字段数；含 `未到场` 与 `已到场` 两类行。
3. 无渠道权限账号打开报名名单页 → 导出时收到「无权访问该资源」类提示（403 由策略层给出），页面不崩。
4. 空活动导出 → 仅表头行。

- [ ] **Step 2: 回填设计文档「上线与验收记录」**

在设计文档末尾追加：

```markdown
## 9. 上线与验收记录

- basic：commit `<本次 commit>`，生产 `/www/apps/strapi` 已 ff-only 到该提交 + `pm2 restart strapi`；插件 dist 已随提交重建。
- web：commit `<本次 commit>`，`h.joho.cn` 已发布。
- 生产验收：未带 token → 401/403；带 token → 200 + `text/csv; charset=utf-8` + BOM `EF BB BF`；表头 9 固定列 + 动态表单列；与 DB `LEFT JOIN` 比对行数与状态一致。
- 已知取舍：不做公式注入前缀（内部使用）、不做导出审计日志、不做 XLSX。
```

- [ ] **Step 3: 回填项目记忆**

在 `project_memory.md` 的「工程惯例」段追加一条（一行，保持该文件风格）：

```markdown
- 【活动名单导出（2026-09-26 上线）】管理端报名名单页可导出 CSV：`GET /api/zhao-point/v1/admin/adm/activities/:documentId/signups/export`（`channelScopeRoute` + `activity.read`，不脱敏）。一行一人，9 固定列（序号/用户ID/昵称/报名状态/到场状态/核销方式/报名时间/到场时间/扣除积分）+ 活动 `formConfig` 动态列；「未到场」由到场记录推导（`cancelled`→已取消优先），不改数据模型。CSV 规格：UTF-8 BOM + CRLF + 全格双引号包裹；时间按 UTC+8 固定偏移格式化。纯逻辑 `zhao-point/server/src/services/signup-export.ts`（含 `csvCell`/`formatDateTimeCst`/`attendanceStatus`/`buildSignupCsv`），单测 `tests/signup-export.test.ts`。前端 `web/src/utils/request.js` 的 `downloadFile(url, params, filename, mime)` 第 4 参可选（默认 xlsx，quiz 模板调用不受影响）。
```

- [ ] **Step 4: 提交文档**

```powershell
git -C e:\code\basic add docs/superpowers/specs/2026-09-26-活动名单导出-design.md docs/superpowers/plans/2026-09-26-活动名单导出.md
git -C e:\code\basic commit -m "docs(activity): 回填活动名单导出设计与上线记录"
git -C e:\code\basic push origin main
```

---

## 自检记录

- **Spec 覆盖**：列定义/状态推导 → Task 1；接口与鉴权 → Task 2；dist 与冒烟 → Task 3；生产验收 → Task 4；前端交互与下载工具 → Task 5；线上验收与回填 → Task 6。
- **命名一致性**：`buildSignupCsv` / `csvCell` / `formatCell` / `formatDateTimeCst` / `attendanceStatus` / `CSV_BOM` / `adminExportSignups` / `adminExportSignups` 路由 `/signups/export` / `exportSignupListCsv` / `downloadFile(..., mime)` 在全部 Task 中写法一致。
- **关系字段铁律**：控制器只用既有 `relId()` 归一，`NaN` 直接跳过；单测含 `{id:N}` 对象形态用例。
- **顺序依赖**：Task 4（basic 上生产）必须在 Task 5（web 发布）之前，避免前端按钮指向未上线接口而 404。
- **未验证前提**：本地 Strapi 是否在 1337 运行；若不可用，Task 3 Step 3 以生产验收等价覆盖（Task 4 不得跳过）。