# 扫码同步领积分 + 问卷飘分 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 活动报名后「领取更多权益」区的问卷领积分加飘分动画；关注公众号改为按用户缓存的临时带参二维码并自动判定已领，手机/电脑同步。

**Architecture:** 活动逻辑落在 zhao-point；复用 zhao-sso 的 `sso-wx-qrcode.create(kind:'temporary')` 生成按 `scene_key=follow:act:{activityId}:{userId}` 复用的用户二维码；关注态沿用 openid 绑定 → `refreshSubscribe` 反查。前端用轮询 `getSignupUnlockStatus` 实现扫码后自动判领（无需微信事件推送）。

**Tech Stack:** Strapi 插件（zhao-point / zhao-sso）、uni-app H5 + 微信小程序、CSS 飘分动画。

---

## 文件结构

- 后端：
  - `basic/plugins/zhao-point/server/src/services/activity.ts` — 新增 `getFollowQrcode` 方法
  - `basic/plugins/zhao-point/server/src/controllers/activity.ts` — 新增 `getFollowQrcode` 控制器
  - `basic/plugins/zhao-point/server/src/routes/content-api.ts` — 注册用户路由
- 前端：
  - `shao/services/api.ts` — 改 `getActivityFollowQrcode` 指向 zhao-point 用户绑定码
  - `shao/pages/activity/detail.vue` — 渲染二维码、问卷飘分动画、扫码轮询同步
- 构建产物：`shao/dist/build/h5`、`basic/plugins/zhao-point/dist`

---

### Task 1: 后端 zhao-point 新增用户关注临时二维码

**Files:**
- Modify: `basic/plugins/zhao-point/server/src/services/activity.ts`
- Modify: `basic/plugins/zhao-point/server/src/controllers/activity.ts`
- Modify: `basic/plugins/zhao-point/server/src/routes/content-api.ts`

- [ ] **Step 1: service 新增 `getFollowQrcode`**

在 `activity.ts`（返回对象的方法区，如 `fillQuestionnaire` 附近之后）新增方法。定位：在方法对象内任意位置加一项即可，注意与其它方法同级（逗号分隔）。

```ts
  /** 用户关注公众号领积分的临时带参二维码（按用户缓存复用，有效期内不重复建码） */
  async getFollowQrcode({ userId, activityId }: { userId: number; activityId: string }) {
    const sceneKey = `follow:act:${activityId}:${userId}`;
    const qrcodeSvc = strapi.plugin("zhao-sso").service("sso-wx-qrcode");
    const existing = await qrcodeSvc.findBySceneKey(sceneKey).catch(() => null);
    if (existing?.wx_url) return { ok: true, wx_url: existing.wx_url };
    const created = await qrcodeSvc.create({
      scene_key: sceneKey,
      kind: "temporary",
      expire_seconds: 2592000,
      title: `活动关注领积分 ${activityId} u${userId}`,
    });
    return { ok: true, wx_url: created.wx_url };
  },
```

- [ ] **Step 2: controller 新增 `getFollowQrcode`**

在 `activity.ts` controller（`/controllers/activity.ts`）的 `subscribe(ctx)` 方法之后新增同级方法：

```ts
  async getFollowQrcode(ctx: any) {
    try {
      const userId = getUserId(ctx);
      const documentId = ctx.params.documentId;
      if (!documentId) {
        ctx.status = 400;
        ctx.body = wrap({ ok: false, reason: "missing_documentId" });
        return;
      }
      const result = await activitySvc().getFollowQrcode({ userId, activityId: documentId });
      ctx.body = wrap(result);
    } catch (e: any) {
      ctx.body = wrap({ ok: false, message: e.message || "生成关注二维码失败" });
    }
  },
```

- [ ] **Step 3: route 注册**

在 `content-api.ts` 的 `/signup/:signupId/unlock-status` 一行（L161）附近新增：

```ts
    userRoute("GET", "/my/activity/:documentId/follow-qrcode", "activity.getFollowQrcode"),
```

- [ ] **Step 4: build 插件验证编译通过**

```bash
cd e:\code\basic\plugins\zhao-point
npx -y @strapi/sdk-plugin build
```

Expected: 无类型错误，生成 `dist/server/index.js`。

---

### Task 2: 前端 api.ts 指向 zhao-point 用户绑定码

**Files:**
- Modify: `shao/services/api.ts:1066-1069`

- [ ] **Step 1: 改写 `getActivityFollowQrcode` 为登录用户临时码**

原实现调 zhao-sso 固定 scene，改为调 zhao-point 用户绑定临时码（需登录）：

```ts
/**
 * 公众号关注临时带参二维码（需登录，按用户缓存复用）
 * @returns { wx_url } 二维码图片地址；未配置公众号时返回 { wx_url: null }
 */
export async function getActivityFollowQrcode(activityId: string) {
  const res = await request(`/zhao-point/v1/my/activity/${activityId}/follow-qrcode`, { method: 'GET' })
  return res?.data ?? res
}
```

---

### Task 3: 前端 detail.vue — 二维码渲染 + 问卷飘分 + 扫码同步

**Files:**
- Modify: `shao/pages/activity/detail.vue`

- [ ] **Step 1: 模板「关注公众号」内联区渲染二维码**

定位模板（约 L224-L230），把纯文字区替换为带二维码的内联区（保留手动领取按钮）：

```html
        <!-- 关注公众号内联领取 -->
        <view v-if="showSubscribeFill" class="benefit-inline">
          <text class="benefit-follow-tip">用微信扫码关注公众号，关注后自动领取 +50 积分及订阅类权益。</text>
          <view class="benefit-follow-qr">
            <image v-if="wxQrcodeUrl" class="benefit-follow-img" mode="aspectFit"
              :src="wxQrcodeUrl" @longpress="saveFollowQrcode" />
            <text v-else class="benefit-follow-empty">暂无可扫二维码</text>
          </view>
          <view class="signup-actions">
            <view class="signup-btn cancel" @click="showSubscribeFill = false"><text>稍后</text></view>
            <view class="signup-btn submit" @click="submitBenefitSubscribe"><text>我已关注，刷新领取</text></view>
          </view>
        </view>
```

在报名信息弹层按钮行之后（模板同层任意位置）加飘分浮层（挂在 benefit-section 内）：

```html
        <!-- +50 飘分动画 -->
        <view v-if="floatText" class="benefit-float" :class="{ show: floatShow }">
          <text class="benefit-float-text">+{{ floatText }} 积分已到账</text>
        </view>
```

- [ ] **Step 2: 关注卡点击时先取码，打开面板**

在 `onBenefitCard`（约 L929）的 `subscribe` 分支改为：打开面板前确保已加载并缓存二维码：

```ts
  if (c.key === 'subscribe') {
    showSubscribeFill.value = !showSubscribeFill.value
    showContactFill.value = false
    if (showSubscribeFill.value && !subscribed.value && !wxQrcodeUrl.value) {
      await loadFollowQrcode()
      subscribeStartPolling()
    } else {
      subscribeStopPolling()
    }
    return
  }
```

- [ ] **Step 3: 问卷提交后刷新权益态并触发飘分**

改写 `submitFillQuestionnaire`（约 L901-L922），成功后 `loadSignupUnlock()` + 触发 +50 飘分：

```ts
async function submitFillQuestionnaire() {
  if (!signupId.value) return
  uni.showLoading({ title: '提交中...' })
  try {
    const res = await fillQuestionnaire(signupId.value, { ...questionnaireData.value })
    uni.hideLoading()
    if ((res as any)?.ok) {
      showQuestionnaire.value = false
      await loadSignupUnlock()
      triggerFloat()   // 飘分（含已领态，见下方实现）
      const newly = (res as any)?.newlyUnlocked
      if (Array.isArray(newly) && newly.length) {
        uni.showToast({ title: `已解锁：${newly.map((g: any) => g.name || '权益').join('、')}`, icon: 'none', duration: 2500 })
      } else {
        uni.showToast({ title: '问卷已提交', icon: 'success' })
      }
    } else {
      uni.showToast({ title: '提交失败', icon: 'none' })
    }
  } catch (e) {
    uni.hideLoading()
    uni.showToast({ title: '提交失败', icon: 'none' })
  }
}
```

- [ ] **Step 4: 新增飘分 + 扫码轮询同步逻辑**

在 script 中（`loadFollowQrcode` 附近）新增状态与方法：

```ts
// ===== +50 飘分动画 =====
const floatText = ref(0)
const floatShow = ref(false)
let floatTimer: any = null
function triggerFloat(points = 50) {
  floatText.value = points
  floatShow.value = false
  nextTick(() => { floatShow.value = true })
  if (floatTimer) clearTimeout(floatTimer)
  floatTimer = setTimeout(() => { floatShow.value = false }, 1600)
}

// ===== 扫码关注自动判定轮询 =====
let subscribeTimer: any = null
async function subscribeStartPolling() {
  subscribeStopPolling()
  if (!signupId.value) return
  subscribeTimer = setInterval(async () => {
    try {
      const st = await getSignupUnlockStatus(signupId.value)
      subscribed.value = !!st?.subscribed
      if (subscribed.value) {
        subscribeStopPolling()
        await submitBenefitSubscribe()
      }
    } catch (e) { /* 忽略单次失败，继续轮询 */ }
  }, 4000)
}
function subscribeStopPolling() {
  if (subscribeTimer) { clearInterval(subscribeTimer); subscribeTimer = null }
}

// 保存二维码到相册（H5 用 downloadFile 提示；小程序 SDK saveImageToPhotosAlbum）
function saveFollowQrcode() {
  if (!wxQrcodeUrl.value) return
  // #ifdef H5
  uni.downloadFile({
    url: wxQrcodeUrl.value,
    success: (r: any) => { uni.saveImageToPhotosAlbum({ filePath: r.tempFilePath }) },
  })
  // #endif
}
```

`submitBenefitSubscribe` 需先在轮询未命中时就判定已领：在 `submitBenefitSubscribe` 成功后调用 `loadSignupUnlock()` 刷新 `subscribed`，且成功路径已存在（L961-964 已有）。确保轮询命中后卡片翻转为「已领 +50」：在 `submitBenefitSubscribe` 的 `res.subscribed` 分支（约 L961）调用 `triggerFloat()`：

```ts
    if ((res as any)?.subscribed) {
      showSubscribeFill.value = false
      await loadSignupUnlock()
      triggerFloat()
      uni.showToast({ title: '已领取关注奖励', icon: 'success' })
    } else {
      uni.showToast({ title: '尚未检测到关注，请先关注公众号', icon: 'none' })
    }
```

统一在卸载时停止轮询（`onUnmounted(subscribeStopPolling)` 已有 onUnmounted 块则并入）。

- [ ] **Step 5: 组件卸载清理**

若组件已有 `onUnmounted`，在其中追加 `subscribeStopPolling()` 与 `if (floatTimer) clearTimeout(floatTimer)`。

---

### Task 4: 前端飘分动画与二维码样式

**Files:**
- Modify: `shao/pages/activity/detail.vue`（`<style scoped>`）

- [ ] **Step 1: 追加样式**

```html
<style scoped>
/* 追加到样式尾部 */
.benefit-follow-qr { display: flex; justify-content: center; align-items: center; padding: 16rpx 0; }
.benefit-follow-img { width: 320rpx; height: 320rpx; }
.benefit-follow-empty { color: #999; font-size: 24rpx; }
.benefit-float {
  position: fixed; left: 50%; top: 32%; transform: translateX(-50%);
  z-index: 200; opacity: 0; pointer-events: none;
}
.benefit-float.show {
  opacity: 1;
  animation: floatUp 1.5s ease forwards;
}
.benefit-float-text {
  display: inline-block; padding: 14rpx 32rpx; border-radius: 999rpx;
  background: #ffe9a8; color: #9a6b00; font-size: 30rpx; font-weight: 600;
  box-shadow: 0 6rpx 20rpx rgba(0,0,0,0.15);
}
@keyframes floatUp {
  0% { transform: translateX(-50%) translateY(0); opacity: 0; }
  15% { transform: translateX(-50%) translateY(-10rpx); opacity: 1; }
  100% { transform: translateX(-50%) translateY(-160rpx); opacity: 0; }
}
</style>
```

（以上为`.benefit-float`/`benefit-flo` 的 scoped 样式块；实际追加时保持既有 `<style scoped>` 结构，只插入这些规则。）

---

### Task 5: 构建与部署验证

**Files:**
- Build: `shao/dist/build/h5`
- Deploy: v.joho.cn 前端 + zhao-point 插件 dist

- [ ] **Step 1: 前端 H5 构建并部署**

```bash
cd e:\code\shao
npm run build:h5
powershell -ExecutionPolicy Bypass -File e:\code\shao\deploy-h5.ps1
```

Expected: 构建成功，`page` 详情 chunk 哈希变化并 `SYNC_OK`。

- [ ] **Step 2: 后端插件部署并重启**

```bash
ssh joho "sudo bash -c 'cd /www/apps/strapi/plugins/zhao-point && rm -rf dist && cp -a /tmp/zpdist ./dist'"
scp -r e:\code\basic\plugins\zhao-point\dist joho:/tmp/zpdist
ssh joho "pm2 restart strapi"
```

Expected: 服务在线，`curl` 关注二维码接口返回 `wx_url`。

- [ ] **Step 3: 重建微信小程序包（可选）**

```bash
cd e:\code\shao
npm run build:mp-weixin   # dist/build/mp-weixin 待微信开发者工具手动上传
```

- [ ] **Step 4: 冒烟验证**

用 Playwright 打开 `https://v.joho.cn/#/pages/activity/detail`（登录态），确认：问卷提交出现飘分与「已领」；关注卡内渲染二维码；PC 无登录态时接口返回 401（进入登录）。记录结果。

---

## Self-Review

- **Spec 覆盖**：问卷飘分(Task3/4)、临时带参二维码(Task1/2/3)、扫码自动判定同步(Task3 轮询)、双端同账号已领(Task3 刷新+轮询)、缓存复用(Task1 findBySceneKey)。✓
- **类型一致**：`getFollowQrcode({userId, activityId})` 在 service/controller 一致；`triggerFloat`/`subscribeStartPolling`/`subscribeStopPolling` 命名前后一致；`wxQrcodeUrl`/`subscribed`/`signupId` 沿用既有 ref。✓
- **占位符**：无 TBD/TODO 占位；所有改动含具体代码。✓