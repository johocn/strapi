# 线下活动缺口补齐计划 4 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 清掉计划 2/3 挂账的剩余尾巴——① 消息任务永久卡死 `sending` 的潜伏活雷；② 到场记录的审计列漏记与 Geo 值域缺失；③ `permissions.test.ts` 21 例长期失败；④ 非通知路径身份解析静默降级无日志。

**Architecture:** 三条独立线：zhao-sso 修「消息任务状态机不变量」（进入 `sending` 后不得再抛错，解析校验一律前置 + 失败即终态）；zhao-point 补「到场审计完整性」（扫码路径补记操作人 + bootstrap 幂等补 Geo CHECK 与外键）；测试与日志为纯修复/可观测性改动，不改业务语义。所有 DB 约束改动走既有 bootstrap 幂等兜底模式，零手工 DDL 脚本。

**Tech Stack:** Strapi 5 插件（zhao-sso / zhao-point / zhao-course）、jest（ts-jest）、knex raw、PostgreSQL（容器 `1Panel-postgresql-pIe0`）

## 已确证事实（2026-09-26 侦察，可直接引用）

1. **卡死点（代码）**：[sso-msg.ts#L230](file:///e:/code/basic/plugins/zhao-sso/server/src/services/sso-msg.ts#L230) 先把 job 置 `status="sending"`，随后 [L232](file:///e:/code/basic/plugins/zhao-sso/server/src/services/sso-msg.ts#L232) `resolveChannel(job.provider)`（未知 provider 抛 `SSO_MSG_400`）与 [L253](file:///e:/code/basic/plugins/zhao-sso/server/src/services/sso-msg.ts#L253) `if (!wxTemplateId) throwErr("SSO_MSG_JOB_500")` **都在 `try` 块之外** → job 永久停留 `sending`。因 [buildJob#L120](file:///e:/code/basic/plugins/zhao-sso/server/src/services/sso-msg.ts#L120) 的幂等判定把 `sending` 视为「未终态」，同一 `dedupeKey` 被**永久占死**，该用户该场景再也收不到消息。
2. **触发条件（生产尚未踩中，属潜伏）**：模板行存在且 `isEnabled=true` 但 `wx_template_id` 为空 **且** 该用户可解析出 openid。生产 `sso_msg_templates` 6 行中 5 行为空（`act_before`/`act_receipt`/`act_repurchase`/`act_noshow_revisit`/`admin_notify`），仅 `act_confirm` 有值。`act_promoted` 整行不存在，`buildJob` L97 在落库前就抛 404，故孤儿未产生——**生产 `sso_msg_jobs` 当前 0 个 `sending`**（分布：pending 5 / quota_limited 7 / sent 17）。另：`admin_notify` 恰是 `sso-sop.notifyAdmins` 使用的模板，属高危路径。
3. **生产到场表现状**：`activity_attendances` 3 行（`lat`/`lng` 越界 0；`operator_id` 悬空 0，其中 2 行为 NULL）；`activity_attendances_signup_lnk` 3 行，孤儿 0。
4. **lnk 列可空是平台默认**：全库 `*_lnk` 表的 FK 列均 `nullable=YES`（如 `activities_belongs_to_series_lnk.activity_id`）→ 对 lnk 强加 NOT NULL 属偏离 Strapi 约定，且每轮启动会被 schema sync 回滚、需 bootstrap 反复重收紧 → **判为过度设计，只做孤儿检测告警**。
5. **`operator_id` 是活列但漏记**：手动核销路径 [activity.ts#L807-L813](file:///e:/code/basic/plugins/zhao-point/server/src/controllers/activity.ts#L807-L813) 传 `operatorId` → 会写入；扫码路径 [activity.ts#L2006](file:///e:/code/basic/plugins/zhao-point/server/src/services/activity.ts#L2006) 调 `checkin({ userId, activityId, method: "worker_scan" })` **漏传 operatorId** → 扫码到场行的 `operator_id` 恒为 NULL（操作人只落在 `activity_checkin_tickets.usedByUserId`）。生产那 2 行 NULL 即此因。`operatorId` 全部来源为 zhao-point 的 `getUserId(ctx)`，按项目 ID 对齐铁律 = `up_users.id`（生产 0 悬空已验证）。
6. **permissions.test.ts 21 例全败的唯一原因**：文件仍按旧结构断言 `PERMISSIONS[k]` 是字符串数组（[permissions.test.ts#L12](file:///e:/code/basic/plugins/zhao-point/tests/permissions.test.ts#L12) 等），而 [permissions.ts](file:///e:/code/basic/plugins/zhao-point/server/src/permissions.ts) 已改为 `Record<string, { allowRoles: string[] }>`；另 [L155](file:///e:/code/basic/plugins/zhao-point/tests/permissions.test.ts#L155) `expect(PERMISSIONS).toHaveProperty("point-config.read")` 把点号当嵌套路径解析。**逐条比对 `permissions.ts` 后确认：测试的语义断言全部仍然成立**，无需改期望值，只需改取值方式。
7. zhao-sso 已有 jest 基建（[tests/jest.config.ts](file:///e:/code/basic/plugins/zhao-sso/tests/jest.config.ts) + 3 个用例），可仿 [sso-user.test.ts](file:///e:/code/basic/plugins/zhao-sso/tests/sso-user.test.ts) 的 mock 风格；[channel/wechat-template.ts](file:///e:/code/basic/plugins/zhao-sso/server/src/services/channel/wechat-template.ts) 构造无副作用（不建连接、不发请求），`MSG_WECHAT_PROVIDER=mock` 可免真调微信。
8. `buildJob` 的 3 个调用点均已 try/catch 包裹并 warn：`sso-sop.trigger`（[L100-L104](file:///e:/code/basic/plugins/zhao-sso/server/src/services/sso-sop.ts#L100-L104)）、`notifyAdmins`（[L180-L191](file:///e:/code/basic/plugins/zhao-sso/server/src/services/sso-sop.ts#L180-L191)）、`dispatchManualTodo`（[L212-L224](file:///e:/code/basic/plugins/zhao-sso/server/src/services/sso-sop.ts#L212-L224)）→ 在 `buildJob` 增加前置抛错**不会**引入未捕获异常。

## 执行总序

Task 1（zhao-sso 消息状态机）→ Task 2（zhao-point 到场审计 + Geo 约束 + fee 日志）→ Task 3（permissions 测试修复）→ Task 4（zhao-course 静默降级补 warn）→ Task 5（部署验证 + 记忆回填）。

Task 1/2/4 各改**不同插件**的 `server/src` 并各自重建 dist，必须**串行**执行（同仓 dist 产物会互相覆盖）。每个 Task 的 `git add` 只列本 Task 涉及文件。

---

### Task 1: zhao-sso 消息任务不再卡死 sending

**Files:**
- Modify: `plugins/zhao-sso/server/src/services/sso-msg.ts`
- Create: `plugins/zhao-sso/tests/sso-msg.test.ts`

- [x] **Step 1.1: 写失败测试**

新建 `plugins/zhao-sso/tests/sso-msg.test.ts`：

```ts
/**
 * sso-msg service 单元测试
 * 重点：模板存在但未配置 wx_template_id（生产常见态）时，任务必须落 failed 终态，
 * 绝不允许留下卡死 sending 的孤儿任务（会永久占死 dedupeKey）。
 */
const mockDb = {
  findOne: jest.fn(),
  findMany: jest.fn(),
  create: jest.fn(),
  update: jest.fn(),
};

const mockStrapi = {
  db: { query: () => mockDb },
  plugin: () => ({ service: () => ({ evaluate: async () => ({ allowed: true }) }) }),
  log: { warn: jest.fn(), info: jest.fn() },
};

import ssoMsgFactory from "../server/src/services/sso-msg";

describe("sso-msg sendJob", () => {
  beforeEach(() => jest.clearAllMocks());

  it("模板缺 wx_template_id 时任务落 failed 而非卡死 sending", async () => {
    const svc = ssoMsgFactory({ strapi: mockStrapi as any });
    mockDb.findOne
      // 第 1 次：sendJob 读 job（模板存在、openid 已解析、但模板无 wx_template_id）
      .mockResolvedValueOnce({
        id: 9,
        status: "pending",
        provider: "wechat",
        scene: "activity.promoted",
        toTarget: "openid_1",
        params: {},
        retryCount: 0,
        user: 5,
        version: null,
        template: { id: 3, code: "act_promoted", wxTemplateId: null, wxTemplateFields: [] },
      })
      // 第 2 次：getJob 回读
      .mockResolvedValueOnce({ id: 9, status: "failed" });

    const job = await svc.sendJob(9);

    const statuses = mockDb.update.mock.calls.map((c: any) => c[0]?.data?.status).filter(Boolean);
    expect(statuses).not.toContain("sending");
    expect(statuses).toContain("failed");
    expect((job as any).status).toBe("failed");
    expect(mockStrapi.log.warn).toHaveBeenCalled();
  });
});

describe("sso-msg buildJob", () => {
  beforeEach(() => jest.clearAllMocks());

  it("微信模板缺 wx_template_id 时拒绝落库", async () => {
    const svc = ssoMsgFactory({ strapi: mockStrapi as any });
    mockDb.findOne.mockResolvedValueOnce({
      id: 3,
      code: "act_before",
      isEnabled: true,
      provider: "wechat",
      wxTemplateId: null,
      wxTemplateFields: [],
    });
    mockDb.findMany.mockResolvedValue([]);

    await expect(
      svc.buildJob({ user: 5, scene: "activity.before", templateCode: "act_before" })
    ).rejects.toThrow(/微信模板ID|wx_template_id/);
    expect(mockDb.create).not.toHaveBeenCalled();
  });
});
```

- [x] **Step 1.2: 跑测试确认失败（红）**

```powershell
npm test -- tests/sso-msg.test.ts
```
（cwd: `e:\code\basic\plugins\zhao-sso`）
Expected：2 例全败——第 1 例因 `sendJob` 直接 reject（`SSO_MSG_JOB_500`）且已先写入 `sending`；第 2 例因 `buildJob` 未抛错。

- [x] **Step 1.3: 实现修复**

3a. 在 [sso-msg.ts#L107-L113](file:///e:/code/basic/plugins/zhao-sso/server/src/services/sso-msg.ts#L107-L113) 的 `picked` 分支之后、`const provider = ...` 之后插入前置校验：

```ts
      const provider = template.provider || "wechat";
      // 前置校验：微信通道必须有模板ID。若放行落库，任务会在发送阶段抛错并永久卡死 sending，
      // 且 buildJob 的幂等判定会把 sending 视为未终态 → 该 dedupeKey 被永久占死。
      // 调用方(trigger/notifyAdmins/dispatchManualTodo)均已 try/catch，故此处抛错是可见且安全的。
      if (provider === "wechat" && !useWxTemplateId) {
        throwErr(
          "SSO_MSG_TEMPLATE_400",
          400,
          `模板 ${templateCode} 缺少微信模板ID(wx_template_id)，拒绝创建消息任务`
        );
      }
```

3b. 重写 `sendJob`，把「渠道解析 / 模板ID解析 / 触达目标解析」三项**全部前置**到 `status="sending"` 之前，失败即置 `failed` 终态；`sending` 之后只剩被 try/catch 包住的 `channel.send`。替换 [L205-L288](file:///e:/code/basic/plugins/zhao-sso/server/src/services/sso-msg.ts#L205-L288) 整段为：

```ts
    /**
     * 发送指定 job（含重试上限），落库回执。
     * 不变量：job 一旦进入 status="sending" 就不得再抛错——否则永久卡死 sending，
     * 且 buildJob 的幂等判定会把 sending 视为未终态，该 dedupeKey 被永久占死。
     * 故渠道/模板ID/触达目标三项解析与校验一律前置，任一失败即置 failed 终态返回。
     */
    async sendJob(jobId: number) {
      const job = await strapi.db.query(MSG_JOB_UID).findOne({
        where: { id: jobId },
        populate: { template: true, version: true, user: true },
      });
      if (!job) throwErr("SSO_MSG_JOB_404", 404, "消息任务不存在");
      if (job.status === "sent") return job;
      if (!job.template) throwErr("SSO_MSG_JOB_500", 500, "任务缺少模板");
      if (job.status === "failed" && job.retryCount >= MAX_RETRY) return job;

      /** 终态化：任何前置校验失败都不留 sending 悬案（failed 为终态，dedupeKey 随之释放） */
      const fail = async (reason: string, message: string, extra: Record<string, any> = {}) => {
        strapi.log.warn(`[zhao-sso:msg] job ${job.id} 置 failed(${reason}): ${message}`);
        await strapi.db.query(MSG_JOB_UID).update({
          where: { id: job.id },
          data: { status: "failed", result: { reason, message, ...extra } },
        });
        return this.getJob(job.id);
      };

      // 触达频控：按用户每日上限 + 场景冷却在发送前拦截，超限置终态 quota_limited
      const qUserId = typeof job.user === "number" ? job.user : job.user?.id;
      const quota = await strapi
        .plugin("zhao-sso")
        .service("sso-quota")
        .evaluate({ userId: qUserId, scene: job.scene, templateId: job.template?.id });
      if (!quota.allowed) {
        strapi.log.warn(`[zhao-sso:msg] sent blocked by quota (user=${qUserId}, scene=${job.scene}): ${quota.reason}`);
        await strapi.db.query(MSG_JOB_UID).update({
          where: { id: job.id },
          data: { status: "quota_limited", result: { reason: quota.reason, scene: job.scene, detail: quota.detail || null } },
        });
        return this.getJob(job.id);
      }

      // 前置 1：渠道可用性
      let channel: any;
      try {
        channel = resolveChannel(job.provider);
      } catch (e: any) {
        return fail("unsupported_provider", e?.message || String(e), { provider: job.provider });
      }

      // 前置 2：模板ID（版本优先取内容，无版本或字段为空回退模板本体）
      const wxFields = job.version?.wxTemplateFields || job.template.wxTemplateFields;
      const wxTemplateId = job.version?.wxTemplateId || job.template.wxTemplateId;
      if (!wxTemplateId) {
        return fail("missing_wx_template_id", "任务缺少模板ID", { template: job.template?.code || null });
      }
      const data = renderData(job.params || {}, wxFields);

      // 前置 3：触达目标(openid)
      let toTarget = job.toTarget;
      if (!toTarget) {
        toTarget = await resolveToTarget(job.user, job.provider);
        if (toTarget) {
          await strapi.db.query(MSG_JOB_UID).update({ where: { id: job.id }, data: { toTarget } });
        }
      }
      if (!toTarget) {
        return fail("no_target", "未解析到触达目标(openid)");
      }

      // 至此全部前置校验通过，才允许进入 sending
      await strapi.db.query(MSG_JOB_UID).update({ where: { id: job.id }, data: { status: "sending" } });

      try {
        const res = await channel.send({
          openid: toTarget,
          templateId: wxTemplateId,
          url: job.link || undefined,
          data,
        });
        await strapi.db.query(MSG_JOB_UID).update({
          where: { id: job.id },
          data: { status: "sent", wxMsgId: String(res.msgId), sentAt: new Date(), result: res.raw || null },
        });
        // 发送成功 → 版本计数（失败/重试不累加）
        if (job.version?.id) {
          await strapi.db.query(VERSION_UID).update({
            where: { id: job.version.id },
            data: { sentCount: (job.version.sentCount || 0) + 1, successCount: (job.version.successCount || 0) + 1, lastUsedAt: new Date() },
          });
        }
      } catch (e: any) {
        const retryCount = (job.retryCount || 0) + 1;
        const retryable = retryCount <= MAX_RETRY && e?.code !== "SSO_MSG_NOT_SUBSCRIBE";
        await strapi.db.query(MSG_JOB_UID).update({
          where: { id: job.id },
          data: {
            status: retryable ? "pending" : "failed",
            retryCount,
            nextRetryAt: retryable ? new Date(Date.now() + RETRY_DELAY_MS) : null,
            result: { error: (e as any).message, code: (e as any).code || null },
          },
        });
      }
      return this.getJob(job.id);
    },
```

- [x] **Step 1.4: 跑测试确认通过（绿）+ 回归**

```powershell
npm test
```
（cwd: `e:\code\basic\plugins\zhao-sso`）
Expected：新增 2 例 PASS；既有 `sso-auth`/`sso-jwt`/`sso-user` 全绿。

- [x] **Step 1.5: 重建 dist 并验证产物**

```powershell
npm run build
Select-String -Path dist\server\index.js -Pattern "missing_wx_template_id"
Select-String -Path dist\server\index.js -Pattern "SSO_MSG_TEMPLATE_400"
```
（cwd: `e:\code\basic\plugins\zhao-sso`）
Expected：两条 Pattern 均命中。无命中 = dist 未重建，必须重跑 `npm run build`。

- [x] **Step 1.6: 提交并推送**

```powershell
git -C e:\code\basic add plugins/zhao-sso/server/src/services/sso-msg.ts plugins/zhao-sso/tests/sso-msg.test.ts plugins/zhao-sso/dist
git -C e:\code\basic commit -m "fix(zhao-sso): 消息任务状态机不变量，模板缺 wx_template_id 不再卡死 sending"
git -C e:\code\basic push origin main
```
**本 Task 不部署**（与 Task 2/3/4 合并为一次部署）。

---

### Task 2: zhao-point 到场审计补记 + Geo 约束 + fee 静默降级日志

**Files:**
- Modify: `plugins/zhao-point/server/src/services/activity.ts`（`redeemCheckinTicket` L2006）
- Modify: `plugins/zhao-point/server/src/bootstrap.ts`（新增两个幂等兜底函数 + 接线）
- Modify: `plugins/zhao-point/server/src/services/fee-service.ts`（`resolveUserProfile` L28-L38）

- [x] **Step 2.1: 扫码核销补记操作人**

在 [activity.ts#L2006](file:///e:/code/basic/plugins/zhao-point/server/src/services/activity.ts#L2006) 把

```ts
    const result = await this.checkin({ userId: signupUserId, activityId: activityDocumentId, method: "worker_scan" });
```

改为

```ts
    // 操作人必须落到到场记录（与手动核销路径对齐），否则 operator_id 恒为 NULL、审计失真
    const result = await this.checkin({
      userId: signupUserId,
      activityId: activityDocumentId,
      method: "worker_scan",
      operatorId: operatorUserId,
    });
```

（`checkin` 内部是 `...(operatorId ? { operatorId } : {})`，`undefined` 时不写入，行为兼容。）

- [x] **Step 2.2: bootstrap 新增 Geo 约束与操作人外键 + lnk 孤儿检测**

在 [bootstrap.ts](file:///e:/code/basic/plugins/zhao-point/server/src/bootstrap.ts) 的常量区（`ATT_LNK_TABLE` 附近）补：

```ts
const ATT_GEO_CHECK = "activity_attendances_geo_range_check";
const ATT_OPERATOR_FK = "activity_attendances_operator_id_fk";
const UP_USERS_TABLE = "up_users";
```

在 `ensureTicketTokenNotNull` 之后、`healIdSequences` 之前插入两个函数：

```ts
/**
 * 幂等补建到场记录的地理值域 CHECK 与操作人外键（列级 schema 不落库，详见文件头注释）。
 * 逐项先查违例：有违例则告警跳过，绝不阻断启动（交人工核账后下一轮启动自动补建）。
 * 说明：operator_id 仅在「手动核销」与「扫码核销」路径写入（adminScanCheckin/svc.redeemCheckinTicket），
 * 值为 up_users.id（zhao-point 的 getUserId 口径）。
 */
const ensureAttendanceGeoAndOperatorConstraints = async (strapi: Core.Strapi) => {
  const knex = strapi.db.connection;

  // ① 经纬度值域：lat∈[-90,90]、lng∈[-180,180]；允许 NULL（C 端未授权定位时不上报）
  const hasGeo = await knex("pg_constraint").where({ conname: ATT_GEO_CHECK }).first();
  if (!hasGeo) {
    const res: any = await knex.raw(
      `SELECT count(*)::int AS n FROM ${ATT_TABLE}
       WHERE (lat IS NOT NULL AND (lat < -90 OR lat > 90))
          OR (lng IS NOT NULL AND (lng < -180 OR lng > 180))`
    );
    const n = Number(((res?.rows ?? res) || [])[0]?.n ?? 0);
    if (n > 0) {
      strapi.log.warn(`[zhao-point] ${ATT_TABLE} 有 ${n} 行经纬度越界，跳过地理值域约束，请先人工核账`);
    } else {
      await knex.raw(
        `ALTER TABLE ${ATT_TABLE} ADD CONSTRAINT ${ATT_GEO_CHECK} CHECK (
           (lat IS NULL OR (lat >= -90 AND lat <= 90)) AND
           (lng IS NULL OR (lng >= -180 AND lng <= 180))
         )`
      );
      strapi.log.info(`[zhao-point] 地理值域约束已创建 (${ATT_GEO_CHECK})`);
    }
  }

  // ② 操作人外键：悬空引用会让审计链失真（Allowed NULL，仅约束非空值的引用有效性）
  const hasFk = await knex("pg_constraint").where({ conname: ATT_OPERATOR_FK }).first();
  if (!hasFk) {
    const res: any = await knex.raw(
      `SELECT count(*)::int AS n FROM ${ATT_TABLE} a
       WHERE a.operator_id IS NOT NULL
         AND NOT EXISTS (SELECT 1 FROM ${UP_USERS_TABLE} u WHERE u.id = a.operator_id)`
    );
    const n = Number(((res?.rows ?? res) || [])[0]?.n ?? 0);
    if (n > 0) {
      strapi.log.warn(`[zhao-point] ${ATT_TABLE}.operator_id 有 ${n} 行悬空引用，跳过操作人外键，请先人工核账`);
    } else {
      await knex.raw(
        `ALTER TABLE ${ATT_TABLE} ADD CONSTRAINT ${ATT_OPERATOR_FK}
           FOREIGN KEY (operator_id) REFERENCES ${UP_USERS_TABLE}(id) ON DELETE SET NULL`
      );
      strapi.log.info(`[zhao-point] 操作人外键已创建 (${ATT_OPERATOR_FK})`);
    }
  }
};

/**
 * lnk 表半链接行检测（只告警，不改结构）。
 * 结论：Strapi 全部 *_lnk 表的 FK 列默认可空（实测 activities_belongs_to_series_lnk.activity_id
 * 亦为 nullable），对其强加 NOT NULL 属偏离平台约定，且每轮启动会被 schema sync 回滚、
 * 需 bootstrap 反复重收紧 → 判为过度设计，仅保留可观测性告警。
 * 正常关系写入不会产生半链接行，出现即说明存在直连 SQL 或手工操作。
 */
const checkLnkOrphans = async (strapi: Core.Strapi) => {
  const res: any = await strapi.db.connection.raw(
    `SELECT count(*)::int AS n FROM ${ATT_LNK_TABLE}
     WHERE activity_signup_id IS NULL OR activity_attendance_id IS NULL`
  );
  const n = Number(((res?.rows ?? res) || [])[0]?.n ?? 0);
  if (n > 0) {
    strapi.log.warn(`[zhao-point] ${ATT_LNK_TABLE} 存在 ${n} 行孤儿链接(半链接行)，正常关系写入不会产生，请人工核账`);
  }
};
```

在 `bootstrap` 中，紧接 `ensureTicketTokenNotNull` 的 try 块之后接线：

```ts
  try {
    await ensureAttendanceGeoAndOperatorConstraints(strapi);
  } catch (err: any) {
    strapi.log.warn(`[zhao-point] 到场记录地理值域/操作人外键创建失败: ${err.message}`);
  }

  try {
    await checkLnkOrphans(strapi);
  } catch (err: any) {
    strapi.log.warn(`[zhao-point] lnk 孤儿检测失败: ${err.message}`);
  }
```

- [x] **Step 2.3: fee-service 静默降级补 warn**

把 [fee-service.ts#L31-L38](file:///e:/code/basic/plugins/zhao-point/server/src/services/fee-service.ts#L31-L38) 的

```ts
        const sso = await sop.resolveSsoUserForUpUser(upUserId);
        if (sso) {
          const profile = await strapi.db.query(SSO_PROFILE_UID).findOne({ where: { user: sso.id } });
          if (profile?.segment) segment = profile.segment;
          const rel = await strapi.db.query(REF_UID).findOne({ where: { inviter: sso.id } });
          isPartner = !!rel;
        }
```

改为

```ts
        const sso = await sop.resolveSsoUserForUpUser(upUserId);
        if (sso) {
          const profile = await strapi.db.query(SSO_PROFILE_UID).findOne({ where: { user: sso.id } });
          if (profile?.segment) segment = profile.segment;
          const rel = await strapi.db.query(REF_UID).findOne({ where: { inviter: sso.id } });
          isPartner = !!rel;
        } else {
          // 身份解析失败会按最低档 C 计价（档位/因子定价可能算错钱），必须可观测
          strapi.log.warn(
            `[zhao-point:fee] 身份解析降级: up_users#${upUserId} 无 sso 映射，按最低档 C/非合伙人计价`
          );
        }
```

- [x] **Step 2.4: 跑 zhao-point 测试 + 重建 dist 并验证**

```powershell
npm test
npm run build
Select-String -Path dist\server\index.js -Pattern "activity_attendances_geo_range_check"
Select-String -Path dist\server\index.js -Pattern "activity_attendances_operator_id_fk"
Select-String -Path dist\server\index.js -Pattern "身份解析降级"
```
（cwd: `e:\code\basic\plugins\zhao-point`）
Expected：`permissions.test.ts` 的 21 例此时**仍然失败**（Task 3 才修，属已知）；其余用例全绿。三条 Select-String 均命中。

- [x] **Step 2.5: 提交并推送**

```powershell
git -C e:\code\basic add plugins/zhao-point/server/src/bootstrap.ts plugins/zhao-point/server/src/services/activity.ts plugins/zhao-point/server/src/services/fee-service.ts plugins/zhao-point/dist
git -C e:\code\basic commit -m "fix(zhao-point): 扫码核销补记操作人到场审计，bootstrap 补 Geo 值域与操作人外键"
git -C e:\code\basic push origin main
```

---

### Task 3: 修复 permissions.test.ts 21 例既有失败

**Files:**
- Modify: `plugins/zhao-point/tests/permissions.test.ts`

**根因（见「已确证事实 6」）**：测试按旧结构取值，`permissions.ts` 已改为 `{ allowRoles }`。**只改取值方式，不改任何期望角色。**

- [x] **Step 3.1: 引入取值 helper**

在 [permissions.test.ts#L1](file:///e:/code/basic/plugins/zhao-point/tests/permissions.test.ts#L1) 的 import 之后插入：

```ts
/** 统一取值口径：PERMISSIONS 的值为 { allowRoles } 结构（历史曾是字符串数组） */
const rolesOf = (permission: string): string[] => PERMISSIONS[permission]?.allowRoles ?? [];
```

- [x] **Step 3.2: 全文替换取值表达式**

把文件内**全部 55 处** `PERMISSIONS['<key>']` 替换为 `rolesOf('<key>')`（等价正则 `PERMISSIONS\[('[^']+')\]` → `rolesOf($1)`）。涉及 key：`point-config.read`/`point-config.update`、`point-rule.create`/`read`/`update`/`delete`、`point.grant`/`point.read`、`point-redeem`、`point-record.read`、`point-product.read`/`create`/`update`/`delete`、`point-redemption.approve`/`read`。
注意**不要**动 `Object.entries(PERMISSIONS)`、`Object.keys(PERMISSIONS)`、`typeof PERMISSIONS` 这几处。

- [x] **Step 3.3: 修 `toHaveProperty` 的点号路径误解析**

把 [L154-L156](file:///e:/code/basic/plugins/zhao-point/tests/permissions.test.ts#L154-L156) 的

```ts
      requiredPermissions.forEach(permission => {
        expect(PERMISSIONS).toHaveProperty(permission);
      });
```

改为（`toHaveProperty("a.b")` 会按嵌套路径解析，对含点号权限码必然失败）：

```ts
      const keys = Object.keys(PERMISSIONS);
      requiredPermissions.forEach(permission => {
        expect(keys).toContain(permission);
      });
```

- [x] **Step 3.4: 补齐活动域权限到完整性清单**

把 [L135-L152](file:///e:/code/basic/plugins/zhao-point/tests/permissions.test.ts#L135-L152) 的 `requiredPermissions` 数组末尾（`'point-redemption.read',` 之后）追加本仓最新补齐的线下活动域权限码，避免再次漂移：

```ts
        'activity.read',
        'activity.create',
        'activity.update',
        'activity.delete',
        'pickup-location.read',
        'pickup-location.create',
        'pickup-location.update',
        'pickup-location.delete',
        'series.read',
        'series.create',
        'series.update',
        'series.delete',
```

- [x] **Step 3.5: 改「角色层级测试」三处解构**

把 [L170-L194](file:///e:/code/basic/plugins/zhao-point/tests/permissions.test.ts#L170-L194) 整段替换为：

```ts
  test('所有权限中的角色都是有效的', () => {
    Object.values(PERMISSIONS).forEach(({ allowRoles }) => {
      allowRoles.forEach(role => {
        expect(validRoles).toContain(role);
      });
    });
  });

  test('admin 角色拥有所有权限', () => {
    Object.values(PERMISSIONS).forEach(({ allowRoles }) => {
      expect(allowRoles).toContain('admin');
    });
  });

  test('user 角色权限最少', () => {
    const userPermissions = Object.entries(PERMISSIONS)
      .filter(([_, entry]) => entry.allowRoles.includes('user'))
      .map(([permission]) => permission);

    expect(userPermissions).toContain('point.read');
    expect(userPermissions).toContain('point-redeem');
    expect(userPermissions).toContain('point-record.read');
    expect(userPermissions).toContain('point-product.read');
    expect(userPermissions).toContain('point-redemption.read');
  });
```

（`validRoles` 常量行保持不动。）

- [x] **Step 3.6: 跑测试确认 23 例全绿**

```powershell
npx jest tests/permissions.test.ts
```
（cwd: `e:\code\basic\plugins\zhao-point`）
Expected：`Tests: 23 passed, 23 total`；不再出现 `roles.forEach is not a function` 或 `Expected path` 失败。

- [x] **Step 3.7: 提交并推送**

```powershell
git -C e:\code\basic add plugins/zhao-point/tests/permissions.test.ts
git -C e:\code\basic commit -m "test(zhao-point): 权限测试对齐 allowRoles 结构，修复 21 例长期失败"
git -C e:\code\basic push origin main
```
**本 Task 无 dist 改动**（仅测试文件），不重建 dist。

---

### Task 4: zhao-course 身份解析静默降级补 warn

**Files:**
- Modify: `plugins/zhao-course/server/src/services/enrollment.ts`（L87-L89、L113-L117）
- Modify: `plugins/zhao-course/server/src/services/course-progress.ts`（L243-L245）

- [x] **Step 4.1: enrollment 埋点路径补 warn**

把 [enrollment.ts#L87-L89](file:///e:/code/basic/plugins/zhao-course/server/src/services/enrollment.ts#L87-L89) 的

```ts
      const sop = strapi.plugin("zhao-sso").service("sso-sop");
      const sso = await sop.resolveSsoUserForUpUser(userId); // 按标识匹配, 匹配不到返回 null
      if (sso) {
```

改为

```ts
      const sop = strapi.plugin("zhao-sso").service("sso-sop");
      const sso = await sop.resolveSsoUserForUpUser(userId); // 按标识匹配, 匹配不到返回 null
      if (!sso) {
        // 无 sso 映射 → course.enrolled 课后SOP排期整体跳过，必须可观测（否则学员静默收不到课中提醒）
        strapi.log.warn(`[course] course.enrolled 埋点跳过: up_users#${userId} 无 sso 映射`);
      }
      if (sso) {
```

- [x] **Step 4.2: enrollment 电商档案回退路径补 warn**

把 [enrollment.ts#L113-L117](file:///e:/code/basic/plugins/zhao-course/server/src/services/enrollment.ts#L113-L117) 的

```ts
      if (!ssoId) {
        const sop = strapi.plugin("zhao-sso").service("sso-sop");
        const sso = await sop.resolveSsoUserForUpUser(userId);
        if (sso?.id) ssoId = String(sso.id);
      }
```

改为

```ts
      if (!ssoId) {
        const sop = strapi.plugin("zhao-sso").service("sso-sop");
        const sso = await sop.resolveSsoUserForUpUser(userId);
        if (sso?.id) ssoId = String(sso.id);
        else strapi.log.warn(`[course] 标记课程用户跳过: up_users#${userId} 无 sso_id 且无 sso 映射`);
      }
```

- [x] **Step 4.3: course-progress 激活提醒补 warn**

把 [course-progress.ts#L243-L245](file:///e:/code/basic/plugins/zhao-course/server/src/services/course-progress.ts#L243-L245) 的

```ts
      const sop = strapi.plugin("zhao-sso").service("sso-sop");
      const sso = await sop.resolveSsoUserForUpUser(userId);
      if (!sso) continue;
```

改为

```ts
      const sop = strapi.plugin("zhao-sso").service("sso-sop");
      const sso = await sop.resolveSsoUserForUpUser(userId);
      if (!sso) {
        // 无 sso 映射 → 该学员本轮激活提醒跳过，必须可观测（否则静默漏发且无痕迹）
        strapi.log.warn(`[course] 激活提醒跳过: up_users#${userId} 无 sso 映射`);
        continue;
      }
```

- [x] **Step 4.4: 跑测试 + 重建 dist 并验证**

```powershell
npm test
npm run build
Select-String -Path dist\server\index.js -Pattern "无 sso 映射"
```
（cwd: `e:\code\basic\plugins\zhao-course`）
Expected：测试全绿（若该插件无测试则 jest 报 no tests 属正常，改跑 `npx tsc --noEmit -p tsconfig.json` 确认无类型错误）；Select-String 命中 3 行以上。

- [x] **Step 4.5: 提交并推送**

```powershell
git -C e:\code\basic add plugins/zhao-course/server/src/services/enrollment.ts plugins/zhao-course/server/src/services/course-progress.ts plugins/zhao-course/dist
git -C e:\code\basic commit -m "fix(zhao-course): 身份解析失败路径补 warn，消除 SOP 静默漏发"
git -C e:\code\basic push origin main
```

---

### Task 5: 部署验证与记忆回填

- [x] **Step 5.1: 部署（Task 1-4 合并一次）**

```powershell
ssh joho "cd /www/apps/strapi && git diff --name-only HEAD origin/main | head -20 && git fetch && git merge --ff-only origin/main"
```
Expected：无本地未提交改动重叠；ff-only 成功。**若出现真冲突立即停止并报告**（不要强推、不要 reset）。

```powershell
ssh joho "export PATH=/home/admin/.nvm/versions/node/v22.23.1/bin:/usr/bin:/bin:$PATH; export PM2_HOME=/home/admin/.pm2; pm2 restart strapi"
```
等待约 60s。

- [x] **Step 5.2: 启动日志验收**

```powershell
ssh joho "export PATH=/home/admin/.nvm/versions/node/v22.23.1/bin:/usr/bin:/bin:$PATH; export PM2_HOME=/home/admin/.pm2; pm2 logs strapi --lines 200 --nostream"
```
Expected：
- 含 `[zhao-point] 地理值域约束已创建 (activity_attendances_geo_range_check)`
- 含 `[zhao-point] 操作人外键已创建 (activity_attendances_operator_id_fk)`
- **不含** `跳过地理值域约束` / `跳过操作人外键` / `孤儿链接` / `修复落后 ID 序列` / `自愈巡检失败`
- **不含** 插件加载报错

- [x] **Step 5.3: 约束落库复核（含「sync 是否会回滚」验证）**

```powershell
$sql = @'
\pset pager off
SELECT conname, pg_get_constraintdef(oid) FROM pg_constraint
WHERE conname IN ('activity_attendances_geo_range_check','activity_attendances_operator_id_fk');
'@
$b64=[Convert]::ToBase64String([Text.Encoding]::UTF8.GetBytes($sql))
ssh joho "echo $b64 | base64 -d | docker exec -i 1Panel-postgresql-pIe0 psql -U youshaop -d strapi"
```
Expected：两条约束都在，CHECK 含 `-90/90` 与 `-180/180`，FK 为 `REFERENCES up_users(id) ON DELETE SET NULL`。
**再重启一次并重复本步**，确认约束在 schema sync 之后依然存在（未被 diff 删除）——这是本次唯一有平台契约风险的改动。

- [x] **Step 5.4: 功能回归**

```powershell
# 1) 手动核销（写入 operator_id + 落审计）
ssh joho "curl -s -X POST localhost:1337/api/zhao-auth/v1/admin/auth/local -H 'Content-Type: application/json' -d '{\"identifier\":\"admin\",\"password\":\"Admin@12345\"}'"
# 取 token 后：
# 2) 扫码核销（验证 operator_id 不再为 NULL）
# 3) 新用户注册返回 201（P0 回归）
```
Expected：① 登录返回 jwt；② 手动/扫码核销各产生一条 `activity_attendances`，两条 `operator_id` 均非空（可用 Step 5.3 同款 SQL 查 `SELECT id, method, operator_id FROM activity_attendances ORDER BY id DESC LIMIT 5`）；③ 新用户注册 201。

- [x] **Step 5.5: 本文件追加「执行记录」小节**

记录 Task 1-4 的 commit hash、测试输出摘要（permissions 23/23 绿、sso-msg 2 例绿）、启动日志关键行、约束复核结论、回归结果。

- [x] **Step 5.6: 提交计划文件**

```powershell
git -C e:\code\basic add docs/superpowers/plans/2026-09-26-线下活动缺口补齐计划4.md
git -C e:\code\basic commit -m "docs: 计划4执行记录回填（消息状态机/到场审计/权限测试/静默降级）"
git -C e:\code\basic push origin main
```

- [x] **Step 5.7: 记忆回填（仓库外文件，不提交）**

更新 `project_memory.md`：
- 新增【消息任务状态机不变量（2026-09-26）】：`sendJob` 进入 `sending` 后不得再抛错的约束；三项解析前置 + `fail()` 终态化；`buildJob` 对空 `wx_template_id` 前置拒绝；**生产 `sso_msg_templates` 5 个模板仍无 `wx_template_id`（act_before/act_receipt/act_repurchase/act_noshow_revisit/admin_notify），需运营在公众号后台申请后补配，补齐前这些场景只有站内信可达**。
- 新增【到场审计口径（2026-09-26）】：`activity_attendances.operator_id` = up_users.id，手动核销与扫码核销两路均已写入；已加 FK 与 Geo 值域 CHECK（bootstrap 幂等）；lnk 半链接行走检测告警而非 NOT NULL（平台默认可空）。
- 更新【测试挂账】：`tests/permissions.test.ts` 21 例已修复（对齐 `allowRoles` 结构），不再挂账。

**验收标准：** `zhao-sso` 与 `zhao-point` 测试全绿（permissions 23/23）；4 个插件 dist 均已重建且含新关键字；生产启动日志显示两条约束创建、无跳过告警、无错误；重启后约束仍在；扫码核销 `operator_id` 非空；新用户注册 201；每个 Task 的 commit 只含本 Task 文件（不碰工作区 5 个他人 CRLF 伪改动文件）。

**明确不做：**
- 不对 `activity_attendances_signup_lnk` 强加 NOT NULL（平台默认 FK 列可空，会与 schema sync 反复拉锯）；
- 不为 `act_promoted` 等模板补 `wx_template_id`（运营在公众号后台申请后才能补，属外部依赖）；
- 不重构 sso-msg 整体架构、不改 `sso_msg_jobs`/`sso_msg_templates` 表结构；
- 不清理生产演练遗留数据与测试用户（`drill_a-d` id 30-33 保留作样例）；
- 不引入任何新依赖。

---

## 执行记录

### 提交清单（全部已 push origin main）

| Task | commit | 范围 |
| --- | --- | --- |
| 计划 | `5001f15240` | 本计划文件 |
| Task 1 | `d75d0aabdb` | `zhao-sso` sso-msg 状态机不变量 + `tests/sso-msg.test.ts` |
| Task 1 加固 | `9e3e4f8f6f` | `tests/sso-msg.test.ts` 改 `resetAllMocks` 隔离 mock 队列 |
| Task 2 | `f5d525ab3e` | `zhao-point` 扫码补 operatorId + bootstrap Geo CHECK/FK + fee 降级日志 |
| Task 3 | `ed833ba6cd` | `zhao-point` `tests/permissions.test.ts` 对齐 `allowRoles` |
| Task 4 | `00c43de06c` | `zhao-course` enrollment / course-progress 补 warn |

### Task 1 — 消息任务状态机不变量

- `buildJob`：`provider === "wechat" && !useWxTemplateId` → 抛 `SSO_MSG_TEMPLATE_400`，**不落库**。
- `sendJob`：渠道解析 / 模板ID解析 / 触达目标(openid)解析三项**前置到 `status="sending"` 之前**，任一失败走 `fail()` 置 `failed` 终态并带 `result.reason`；`sending` 之后仅剩被 try/catch 包裹的 `channel.send`。
- 测试：`npx jest --config tests/jest.config.ts` → **31/31 绿**（含新增 2 例）。
- 副作用（已知并接受）：`sso-sop.notifyAdmins` 使用的 `admin_notify` 模板无 `wx_template_id`，此前 `buildJob` 会成功建 job 使 `notified++`，现在抛错被 catch → `notified` 恒为 0。调用方 `enqueueManualSop`/`trigger` 均已 try/catch，**无未捕获异常**。

### Task 2 — 到场审计 + 列约束

- 扫码核销 `activity.ts:2006` 补 `operatorId: operatorUserId`（与手动核销路径对齐）。
- `bootstrap.ts` 新增 `ensureAttendanceGeoAndOperatorConstraints`（幂等：先查 `pg_constraint.conname`，Geo 有越界行则 warn 跳过，FK 有悬空引用则 warn 跳过）与 `checkLnkOrphans`（仅告警，不做 DDL）。
- 生产证据：演练数据 `att#5`（`worker_scan`）`operator_id` 为 NULL、`att#7`（`manual`）为 1，与「扫码路径漏传」判断吻合。

### Task 3 — permissions.test.ts

- 全文对齐 `PERMISSIONS[x].allowRoles` 结构；`requiredPermissions` 追加 12 个活动域权限码。
- 计划遗漏 2 处由子代理上报并修正：`point-redeem` 自首个提交即为 `['user']` 专属，原「admin 拥有所有权限」断言先天为假 → 改为「除用户专属权限外」+ `USER_ONLY_PERMISSIONS` 豁免清单 + 守卫用例；`Array.isArray(value)` 同步对齐为 `value.allowRoles`。
- 结果：`permissions.test.ts` **24/24 绿**，`zhao-point` 全量 **96/96 绿**。

### Task 4 — zhao-course 静默降级

- `enrollment.ts` 两处、`course-progress.ts` 一处补 `strapi.log.warn`，映射缺失不再静默跳过。

### Task 5 — 部署与约束验证

- 服务器 `ff-only` 合并至 `00c43de06c`，无冲突；重启成功。
- 启动日志关键行：
  - `[zhao-point] 地理值域约束已创建 (activity_attendances_geo_range_check)`
  - `[zhao-point] 操作人外键已创建 (activity_attendances_operator_id_fk)`
  - 无「跳过」告警、无「修复落后 ID 序列」、无插件加载报错、`Strapi started successfully`
- `pg_constraint` 复核：CHECK 含 `-90/90` 与 `-180/180`；FK 为 `REFERENCES up_users(id) ON DELETE SET NULL`。
- **二次重启后两条约束均存活** → 确认 Strapi schema sync 不会 diff 掉这两类对象（本次唯一平台契约风险已清除）。
- 事务内双向验证（全部 ROLLBACK，零生产残留）：
  - 合法写入 `operator_id=1, lat=31.2345, lng=121.4737` → 通过；
  - `lat=999` → `ERROR: ... violates check constraint "activity_attendances_geo_range_check"`；
  - `operator_id=999999` → `ERROR: ... violates foreign key constraint "activity_attendances_operator_id_fk"`；
  - 结束后 `activity_attendances` 行数与 `operator_id` 非空行数均未变。
- `sso_msg_jobs` 状态分布：pending 5 / quota_limited 7 / sent 17，**0 个 sending**（无卡死孤儿）。
- 管理端登录成功取到 jwt（`user.id=1, email=admin@example.com, zhaoRoles=["channel-admin"]`）。
- Step 5.4 回归范围说明：管理端登录已验证；**扫码核销未走真票回归**（需先造有效 ticket token，成本高于收益），该改动的正确性由「代码 diff 已确认 `operatorId` 入参」+「DB 层 FK 已生效并拒绝悬空 operator_id」两条共同覆盖。

### 待办 / 外部依赖（不阻塞本次交付）

- **微信模板 ID 缺失（运营外部依赖）**：生产 `sso_msg_templates` 6 行中 5 行 `wx_template_id` 为空（`act_before`/`act_receipt`/`act_repurchase`/`act_noshow_revisit`/`admin_notify`），仅 `act_confirm` 有值；`act_promoted` 整行不存在。补齐前这些场景**仅站内信可达**，需运营在公众号后台申请模板后补配。
- **zhao-course 既有测试债**：`npm test` 13/17 套件失败（67 例），根因为 jest 无法转译 TS 测试文件且用例引用不存在的 `course-tag`/`knowledge-point`/`has-permission` 模块——与本次改动无关，超出计划范围，未修。