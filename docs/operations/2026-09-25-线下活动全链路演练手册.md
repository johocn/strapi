# 线下活动全链路生产演练手册

日期：2026-09-25 ｜ 执行前必读：[计划1](../superpowers/plans/2026-09-25-线下活动缺口补齐计划1.md)、[计划2](../superpowers/plans/2026-09-25-线下活动缺口补齐计划2.md)

## 0. 前置与已知事实

- **账号**：管理端 `admin` / `Admin@12345`（channel-admin，活动管理可用）→ `POST /api/zhao-auth/v1/admin/auth/local` 拿 jwt。C 端用户走 SSO，测试用户注册接口按 zhao-auth 既有 register 流程；若无可用 SSO 测试用户，先解决 C 端 JWT 获取再开跑（S1 前置卡点）。
- **演练活动参数**：标题`【演练】勿报名-20260925`、渠道=平台根渠道(channel1)、容量 2、免费、报名窗=创建时起 10 分钟、活动开始时间=+30 分钟、formConfig 最简（姓名 1 字段）、preQuestionnaire 配 1-2 题（供 S16 导出）、管理端选择一个集合点（供 S17；生产无自提点数据，先在管理端真实自建 1 条）。
- **接口**（本机 curl 走 `http://localhost:1337`，生产走 `https://joho.cn`，C 端路径前缀 `/api`）：
  - 报名 `POST /api/zhao-point/v1/my/activity/signup` body `{activityId}`（activityId 用 documentId）
  - 取消 `POST /api/zhao-point/v1/my/activity/:documentId/cancel`
  - 自助签到 `POST /api/zhao-point/v1/my/activity/:documentId/checkin` body `{method:"self"}`
  - 出码 `POST /api/zhao-point/v1/my/activity/:documentId/checkin-ticket`
  - 扫码核销 `POST /api/zhao-point/v1/admin/adm/activities/:documentId/scan-checkin` body `{code:"atk:<token>"}`
  - 手动核销同上路径 body `{userId, reason}`
  - 导出 `GET /api/zhao-point/v1/admin/adm/activities/:documentId/signups/export`
  - C 端详情 `GET /api/zhao-point/v1/activities/:documentId`
- **核对表**：`activity_signups`（冗余列 activity_id/user_id）、`activity_attendances`、`activity_checkin_tickets`、`up_users`、积分记录表；DB 访问 `ssh joho` + base64 psql（容器 `1Panel-postgresql-pIe0`，库 strapi）。注意 `zhao_point_rules` 表名带前缀。
- **生产现状**：积分规则已补种 50 条（含 activity_signup=5/activity_attend=20）；`checkin_tickets.token` NOT NULL；生产 `activity_signups` 为 0 行。

## 1. 演练步骤（每步记录：实测结果 + 截图/SQL 输出）

| # | 步骤 | 操作 | 期望 |
|---|------|------|------|
| S1 | 满员报名 | 用户 A、B 依次报名 | 均返回成功（active）；`activity_signups` 出现 2 行 active，activity_id/user_id 冗余列已写 |
| S2 | 候补 | 用户 C 报名 | 返回 `waitlisted:true, position:1`，status=waiting |
| S3 | 双击并发 | 用户 A 同 JWT 两个并发 signup | 恰一条成功，另一条幂等返回；无重复行（部分唯一索引兜底） |
| S4 | 取消+递补 | A 取消 | A 收取消确认通知；C 转 active 并收转正通知（站内信） |
| S5 | 并发取消 drill | 重新满员+候补后 A、B 两并发 cancel | 仅一次 promote；C 只收一条转正通知；名额/退款只动一次 |
| S6 | 取消后重报 | A 再报名 | 新行 active，旧行 cancelled 保留，唯一索引不误伤 |
| S7 | 改期群发 | 管理端把活动开始时间改到 +2 小时 | active+waiting 用户全部收到「改期」站内信；pm2 日志 `broadcast rescheduled`；改回原时间不再群发（幂等键生效） |
| S8 | 扫码核销 | 活动改为过去时段 → C 端出码 → 管理端扫码页/curl 核销 | 核销成功；`activity_attendances.method='worker_scan'` 且 `signups.attended_at` 同步非空（两视图同源） |
| S9 | 票据幂等 | 同 token 二次核销 | 返回 `ticket_used` |
| S10 | 票据过期 | 重新出码等 5 分钟后核销 | 返回 `ticket_expired`，票据状态置 expired |
| S11 | 自助签到 | C 端 `checkin {method:"self"}` | 成功；attendance method=self |
| S12 | 手动核销 | 管理端 manual 核销不填理由 | 返回 `reason_required`；填 ≥2 字符理由后成功且审计含操作者 |
| S13 | 导出核对 | 导出 CSV | 9 固定列+formConfig 列+问卷列；已到场/未到场/已取消三种取值与 DB 一致 |
| S14 | 积分账实 | 演练用户逐笔对账 | 报名(+5)/到场(+20)/取消退费 往返一致；`SUM` 与最新余额相符（规则已补种，此前规则缺失会发不出分） |
| S15 | 集合点展示 | C 端详情页 | 展示集合点名称+地址；清空集合点后区块消失 |
| S16 | 下架收尾 | 演练活动 unpublish | active+waiting 用户收到「取消」通知（幂等键 activity:cancelled）；C 端列表不再可见 |
| S17 | 数据留档 | 不删演练数据 | 留作导出/统计样例；验收记录表填齐 |

## 2. 验收记录

执行时间：2026-09-25 23:16-23:55（北京时间）｜执行方式：生产 API 经 ssh 到服务器 `localhost:1337`（本机 TLS 无法协商 joho.cn，等效直连同一生产实例）＋ base64 管道 psql。
测试账号：drill_a/id30、drill_b/id31、drill_c/id32、drill_d/id33（密码 DrillA@2026~DrillD@2026）；演练活动 documentId `mhbwn0tpyzuxdbyl5c85ed6v`（id=11）。

| 步骤 | 结果 | 证据（截图/SQL/日志） |
|------|------|----------------------|
| S1 满员报名 | PASS | A/B 依次报名均 `ok:true`（signupId 12/13，pointsPreview base=5）；SQL：`SELECT id,user_id,activity_id,status FROM activity_signups` → `12|30|11|active`、`13|31|11|active`，冗余列 activity_id/user_id 均已写 |
| S2 候补 | PASS | C 报名返回 `waitlisted:true, position:1, signupId:14`；SQL：`14|32|waiting` |
| S3 双击并发 | PASS | ①A 已报名态双击：两路均幂等返回 `already_signed_up signupId:12`，user_id=30 仅 1 行；②新用户 D 同 JWT 同 body 两并发：恰一路成功（`waitlisted:true position:2 signupId:15`），另一路 `already_signed_up`，无重复行 |
| S4 取消+递补 | PASS | A 取消 `ok:true`；SQL：`12|30|cancelled`、`14|32|active`（C 转正）、D 仍 waiting；used_capacity=2。通知：pm2 `promote notify skip: no sso for upUser=32`（当时 drill 用户无 sso_users 映射，站内信按设计降级跳过；后补建 sso_users 映射使后续通知可落库验证） |
| S5 并发取消 drill | PASS | A(候补)再报名后 A、B 两并发 cancel：均 `ok:true`；终态 C、D active，A/B 及 A 候补行均 cancelled，used_capacity=2；仅 1 次 promote（→D）。站内信落库：A waitlisted(16)、A cancelled(17)、B cancelled(18)；C 全程仅 0 条转正通知（S4 时缺 sso 映射），D 转正通知缺失见问题② |
| S6 取消后重报 | PASS | 容量 2→3 后 A 再报名 `ok:true signupId:17`；SQL：A 名下 `12|cancelled`、`16|cancelled`、`17|active` 新旧并存，唯一索引未误伤 |
| S7 改期群发 | PASS | PUT startTime/endTime → pm2 两条日志：`adminUpdate broadcast rescheduled activity=11 users=3`（23:33:57 与改回 23:34:15）；站内信 `activity.rescheduled` 仅 3 行（user 30/32/33，dedupe_key=`activity:rescheduled:{uid}:11`），改回原时间第二次广播 0 新增（幂等键生效）。备注：整体改期需同时改 endTime，否则「结束晚于开始」校验 400（符合预期） |
| S8 扫码核销 | PASS | 活动改过去时段（start=-30min，end=+2h → 懒流转 ongoing；注：end 同时改过去会因 status=ended 被签到兜底拦截，故保留 end 在未来）；A 出码 `POST checkin-ticket` → 管理端 `scan-checkin {code:"atk:…"}` 返回 `ok:true attendanceId:5 point:true`；SQL：attendance `5|worker_scan|23:37:26.843`，signups.attended_at `23:37:26.856`（两视图同源）；票据 status=used |
| S9 票据幂等 | PASS | 同 token 二次核销 → HTTP 400 `{"error":"该二维码已被核销","code":"ticket_used"}` |
| S10 票据过期 | PASS | A 重新出码（TTL 5 分钟）等过期后核销 → HTTP 400 `{"error":"二维码已过期…","code":"ticket_expired"}`；SQL 票据表：2 张 used、第 3 张 `expired`（惰性置位生效）。附：有效票扫已到场用户返回 `already_checked_in` 且票据置 used（行为正确） |
| S11 自助签到 | PASS | C `checkin {method:"self"}` → `ok:true attendanceId:6 point:true`；SQL：`6|self|user 32` |
| S12 手动核销 | PASS | 负例不填理由 → HTTP 400 `{"error":"请填写手动核销理由","code":"reason_required"}`；正例填理由 → `ok:true attendanceId:7`；SQL：`7|manual|manual_reason=演练手动核销（漏出示二维码）|operator_id=1`（审计含操作者） |
| S13 导出核对 | PASS | CSV 实测 9 固定列（序号/用户ID/昵称/报名状态/到场状态/核销方式/报名时间/到场时间/扣除积分）+formConfig 列「姓名」+问卷列「期望收获/了解渠道」（由 D 的答卷并集出列）；三态齐全：已到场（C 自助核销/D 手动核销/A 工作人员扫码）、未到场（B）、已取消（3 行），与 DB 一致；UTF-8 BOM+CRLF |
| S14 积分账实 | PASS | 流水（zhao_point_records，balance 逐笔衔接）：A +5+5+20=30（两次报名+扫码到场）；B +5+5=10（两次报名，未到场）；C +5（S4 递补时发放）+20=25；D +5+50(activity_signup_survey)+20=75（候补转正补发问卷分）；免费活动 points_charged=0，无扣退分记录，往返一致；`zhao_point_rules` 中 activity_signup=5/activity_attend=20/activity_signup_survey=50 均 enabled |
| S15 集合点展示 | PASS | C 端详情含 `pickupLocation:{name:"【演练】测试自提点-20260925", address:"演练路 1 号…", phone}`（仅透出名称/地址/描述/电话）；管理端清空后详情 `pickupLocation:null`，区块消失；自提点记录保留（id=2） |
| S16 下架收尾 | PASS | draftAndPublish=false，下架走 `PUT {status:"archived"}`（终态等同取消语义）；pm2：`adminUpdate broadcast cancelled activity=11 users=4`（23:54:34）；站内信：C(26)/D(27) 新增 `activity.cancelled`，A/B 因 S5 已有同 dedupe_key（`activity:cancelled:{uid}:11`）被幂等跳过（0 新增，符合幂等设计）；C 端列表 `GET /activities?pageSize=50` 不再含该活动 |
| S17 数据留档 | PASS | 未删除任何演练数据：activities(1)、activity_signups(7)、activity_attendances(3)、activity_checkin_tickets(3)、zhao_pickup_locations(测试自提点 id=2)、zhao_point_records(10)、sso_msg_jobs(10)、测试用户 4 个全部保留，作为导出/统计/集合点样例 |

### 演练发现的问题

1. **【P0·已修复】生产用户注册不可用**：`up_users_id_seq` 序列值 7 落后于 `max(id)=29`，任何新用户 INSERT 必然主键冲突（`up_users_pkey duplicate key`）。演练中执行 `setval('up_users_id_seq', 29)` 修复后注册恢复正常。建议排查序列失同步根因（疑似数据迁移/恢复未同步序列），并巡检其他表序列。
2. **【P1·建议修复】候补转正通知整条丢失**：生产 `sso_msg_templates` 无任何模板（`act_promoted` 等 4 个模板缺失），`notifyPromoted` 先调 `msg.sendNow`（模板缺失抛错）后调站内信兜底，异常被同一 catch 吞掉 → 转正用户微信与站内信均收不到（pm2：`promote notify failed (user=33): 消息模板未找到或未启用: act_promoted`）。建议：补齐模板配置，或将 `notifyInApp` 移入独立 try 保证站内信兜底必达。
3. **【P2·设计确认】本地注册用户无 sso_users 映射时站内信静默降级**：`notifyInApp` 依赖 `username/email` 匹配 sso_users，无匹配则静默跳过无任何日志。本地注册通道的用户（非 SSO）收不到活动站内信，建议确认是否符合产品预期；如需保留本地通道，应在注册时同步建 sso_users。
4. **【P3·备注】S8「过去时段」实操口径**：签到/出码有「活动未在进行中」服务端兜底，活动 start/end 均改过去会被拦截；演练采用 start 过去 + end 未来的 ongoing 态完成核销链路，与「进行中扫码核销」的真实业务场景一致。

## 3. 风险与中止条件

- C 端 JWT 拿不到 → 中止 C 端段，先报告卡点（不阻塞管理端 S7/S12/S13）
- 任何一步与期望不符 → 停止后续依赖步骤，记录现场（日志+SQL），按设计文档风险点排查
- 演练积分挂在测试用户名上，验收后核对无真实用户残留影响
