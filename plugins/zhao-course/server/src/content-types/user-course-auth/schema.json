{
  "kind": "collectionType",
  "collectionName": "zhao_user_course_auths",
  "info": {
    "singularName": "user-course-auth",
    "pluralName": "user-course-auths",
    "displayName": "用户课程授权"
  },
  "options": {
    "draftAndPublish": false
  },
  "attributes": {
    "user": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "plugin::users-permissions.user"
    },
    "course": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "plugin::zhao-course.course"
    },
    "activityDocumentId": {
      "type": "string"
    },
    "lessonDocumentId": {
      "type": "string"
    },
    "authType": {
      "type": "enumeration",
      "enum": ["free", "paid", "admin_grant", "temp_lesson"],
      "default": "free"
    },
    "source": {
      "type": "enumeration",
      "enum": ["signup", "milestone", "manual"],
      "default": "manual"
    },
    "grantedAt": {
      "type": "datetime"
    },
    "expiresAt": {
      "type": "datetime"
    },
    "isExpired": {
      "type": "boolean",
      "default": false
    },
    "channel": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "plugin::zhao-channel.channel"
    },
    "deletedAt": {
      "type": "datetime",
      "default": null
    }
  }
}
